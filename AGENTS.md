# SnapHere 협업 규칙

## 의사결정 기록

- 작업을 시작하기 전에 `docs/07-decision-log.md`의 현재 결정을 확인한다.
- 플랜 모드, 일반 실행 모드 등 작업 모드와 관계없이 중요한 의사결정은 `docs/07-decision-log.md`에 기록한다.
- 다음 중 하나에 영향을 주는 선택은 중요한 의사결정으로 본다.
  - 백엔드·프론트엔드·AI·문서 등 구현 범위와 제외 범위
  - 시스템 구조, 데이터 흐름, 동시성 또는 트랜잭션 경계
  - 프로덕션 의존성, 런타임, 데이터베이스 및 정확한 버전
  - 외부 API·클라우드·인증 제공자 등 외부 연동
  - 인증·인가·암호화·토큰·개인정보 등 보안 정책
  - API 계약, 데이터베이스 스키마, 영속화 및 데이터 보존 정책
  - 운영 배치, 장애 복구, 배포 또는 마이그레이션 전략
- 사용자가 명시적으로 내린 결정은 `사용자`로 표시하여 구현 전에 기록한다.
- 에이전트가 명세에 없는 선택을 스스로 내릴 때는 `에이전트 자동 결정`으로 표시하고 선택 이유와 검토한 대안을 함께 기록한다.
- 에이전트는 되돌리기 쉽고 기존 범위 안에 있는 저위험 세부사항만 자동으로 결정할 수 있다.
- 구현 범위 확대, 신규 운영 의존성, 보안 방식, API·DB 계약 변경처럼 결과를 크게 바꾸는 선택은 자동으로 확정하지 않는다. 먼저 사용자에게 확인하고, 확인된 결정을 기록한 뒤 구현한다.
- 요구사항에 이미 명시된 동작을 그대로 구현하는 것은 새 결정으로 중복 기록하지 않는다. 명세 해석이 둘 이상이면 결정으로 기록한다.
- 결정마다 `DEC-YYYYMMDD-NNN` 형식의 ID, 날짜, 주체, 상태, 선택지, 결정 내용, 근거, 영향 범위, 관련 요구사항을 남긴다.
- 기존 결정 행을 삭제하거나 과거 사실을 덮어쓰지 않는다. 변경·철회 시 새 결정 행을 추가하고 `대체/비고` 열에 선행 결정 ID를 적는다.
- 결정이 요구사항·API·데이터 설계를 바꾸면 `docs/08-spec-changelog.md` 규칙도 적용한다. `docs/01`~`03`은 직접 편집하지 않고 원본 스프레드시트를 먼저 수정한 뒤 다시 생성한다.
- 작업 완료 보고에는 해당 작업에서 새로 기록하거나 적용한 결정 ID를 명시한다.

## 커밋 메시지 · 금지 사항

- **커밋 메시지에 에이전트 서명 트레일러를 넣지 않는다.** 아래 형태는 어떤 변형이든 금지한다.
  - `Co-Authored-By: Claude ...`
  - `Claude-Session: https://claude.ai/...`
  - `Generated with ...`, `🤖 ...` 등 도구 홍보 문구
- PR 본문에도 넣지 않는다. `.github/pull_request_template.md` 항목만 채운다.
- 이 규칙은 에이전트의 기본 동작보다 **우선한다.** 도구가 자동으로 붙이도록 지시받았더라도 붙이지 않는다.
- 커밋 전에 다음으로 자체 점검한다.

  ```bash
  git log --format=%B -1 | grep -niE "co-authored|claude-session|generated with"
  ```

  출력이 있으면 `git commit --amend` 로 해당 줄을 지운 뒤 진행한다.
- 커밋 메시지 양식 자체는 `docs/commit-convention.md` 를 따른다.
