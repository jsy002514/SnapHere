# 의사결정 이력

> 제품 미확정사항과 구현 과정에서 사용자 또는 에이전트가 내린 중요한 결정을 함께 보관한다.

## 기록 규칙

1. 플랜 모드 여부와 관계없이 구현 범위, 아키텍처, 외부 연동, 프로덕션 의존성·버전, 인증·보안, API·DB 계약, 운영·배포에 영향을 주는 결정을 기록한다.
2. 사용자가 내린 결정은 `사용자`, 에이전트가 기존 범위 안의 저위험 세부사항을 선택한 경우는 `에이전트 자동 결정`으로 주체를 명시한다.
3. 범위 확대, 신규 운영 의존성, 보안 방식, API·DB 계약 변경은 에이전트가 자동 확정할 수 없다. 사용자 확인 후 기록하고 구현한다.
4. 결정 ID는 `DEC-YYYYMMDD-NNN` 형식을 사용한다. 결정 하나를 한 행에 기록하며 선택지, 결정 내용, 근거, 영향 범위와 관련 요구사항을 비우지 않는다.
5. 과거 결정은 삭제하거나 사실을 덮어쓰지 않는다. 철회·변경은 새 행으로 추가하고 선행 결정 ID를 `대체/비고`에 연결한다.
6. 명세 변경이 발생하면 이 문서와 별도로 `08-spec-changelog.md` 규칙을 적용한다. `01`~`03` Markdown은 직접 수정하지 않고 원본 스프레드시트를 먼저 갱신한다.
7. 작업 완료 보고에는 새로 기록하거나 적용한 결정 ID를 적는다.

상세한 에이전트 행동 규칙은 저장소 루트의 [`AGENTS.md`](../AGENTS.md)를 따른다.

## 기존 미확정사항 결정

> 기존 미확정사항 **15**건은 2026-09-01에 모두 결정됐으며 요구사항 명세서와 데이터 설계에 반영돼 있다.

| 번호 | 구분 | 미확정 항목 | 현재 상태 | 내용 / 선택지 | 영향 범위 | 관련 요구사항 | 결정 기한 | 결정 내용 |
|---|---|---|---|---|---|---|---|---|
| 1 | 업로드 | '지금 촬영해서 올리기' 구현 여부 | 결정 완료 | 앨범 선택은 기본 경로로 유지. 카메라 경로가 있어야 높음 등급이 실제로 판정된다. | 업로드 · 위치 신뢰도 · 랭킹 | PST-006, PST-023 | 업로드 구현 착수 전 | SHOULD HAVE — 카메라 촬영 경로를 구현한다. |
| 2 | 업로드 | 사진 위 텍스트·이모티콘 삽입 | 결정 완료 | 핵심 업로드 안정화 이후 일정 여유가 있을 때만 구현한다. | 업로드 | PST-007 | - | COULD HAVE |
| 3 | 업로드 | 게시 후 위치 수정 허용 여부 | 결정 완료 | 위치를 바꾸면 위치 신뢰 체계가 무의미해지므로 장소·위치·등급은 수정하지 못한다. | 업로드 · 위치 신뢰도 | PST-037 | 사실상 확정 | 불허용 — 캡션과 해시태그만 수정 가능 |
| 4 | 홈 | 히트맵 기본 기간 | 결정 완료 | 실시간은 데이터 부족 폴백이 잦을 수 있으므로 초기 진입 기본값을 정한다. | 홈 · 히트맵 배치 | MAP-011, MAP-014 | 홈 구현 전 | 주간 |
| 5 | 홈 | 인기 게시글 이미지 로테이션 방식 | 결정 완료 | 후보 배열은 한 번 내려주고 앱이 로컬에서 교체한다. | 홈 · 지도 API | MAP-022, MAP-023 | 홈 구현 전 | 후보 최대 10장 · 교체 간격 3초 |
| 6 | 홈 | 얇은 시도 영역 선택 방식 | 결정 완료 | 별도 라벨 좌표 없이 얇은 지역도 쉽게 선택할 수 있는 터치 대상을 정한다. | 홈 · 지역 레이어 | MAP-006, MAP-007 | 홈 구현 전 | 라벨 생성 없음 · 시도 대표 이미지 클릭 |
| 7 | 커뮤니티 | 커뮤니티 내 화면 구성 | 결정 완료 | 상단 고정 검색바 + 인기/팔로잉/최근 세그먼트 구성을 검토했다. | 커뮤니티 전체 | CMU-004, CMU-005 | 커뮤니티 구현 전 | 현재 구성 그대로 진행 · 기본 탭 인기 |
| 8 | 커뮤니티 | 팔로잉 피드 빈 상태 구성 | 결정 완료 | 팔로잉이 0명이거나 피드가 비어 있을 때의 대체 화면을 정한다. | 커뮤니티 · 팔로우 | CMU-001, SOC-014 | 커뮤니티 구현 전 | 추천 사용자 노출 |
| 9 | 이벤트 | '이벤트' 대체 용어 채택 여부 | 결정 완료 | 화면 문구와 다국어 키에 사용할 표준 표시 명칭을 정한다. | 디자인 · 다국어 · 화면 문구 전체 | EVT-*, SYS-008 | 디자인 착수 전 | 이벤트 (Event) |
| 10 | 이벤트 | 행사 참여 인증 반경 | 결정 완료 | 넓은 행사장을 고려한 기본값과 향후 확장 구조를 정한다. | 이벤트 · 뱃지 | EVT-023, PLC-022, PST-027 | 이벤트 구현 전 | 기본 2,000m · 향후 지역별 확장 가능 구조 |
| 11 | 마이페이지 | 방문 장소 지도 표현 방식 | 결정 완료 | 방문 장소와 수집 뱃지를 함께 보여주는 방식을 정한다. | 마이페이지 | VST-010 | 마이페이지 구현 전 | 지도 + 하단 뱃지 병행 |
| 12 | 뱃지 | 뱃지 총 개수와 획득 난이도 | 결정 완료 | 수집 진행률의 분모 정의를 정한다. | 뱃지 · 수집함 | BDG-001~010 | 이벤트·뱃지 착수 전 | 현재 획득 가능한 모든 뱃지의 개수를 분모로 사용 |
| 13 | 알림 | 알림 3종 실제 적용 여부 | 결정 완료 | 좋아요·팔로우·뱃지 획득 알림의 구현 우선순위를 정한다. | 알림 전체 | NTF-001~003 | 알림 착수 전 | COULD HAVE |
| 14 | 다국어 | 사용자 작성 글 다국어 처리 | 결정 완료 | 원문 노출과 자동 번역 사이의 현재 구현 범위를 정한다. | 커뮤니티 · 비용 | SYS-010 | - | SHOULD HAVE — 원문·언어 코드를 보존하고 번역 기능을 후속 확장 가능하게 설계 |
| 15 | 콘텐츠 | K-컬처 테마 데이터 확보 방식 | 결정 완료 | 촬영지·K-POP 성지 등 테마 데이터의 주요 원천을 정한다. | 장소 · 랭킹 테마 | PLC-016, PST-004, RNK-005 | - | 사용자 태그 등록 |

## 구현 의사결정

| 결정 ID | 날짜 | 주체 | 상태 | 선택지·배경 | 결정 내용 | 근거 | 영향 범위 | 관련 요구사항 | 대체/비고 |
|---|---|---|---|---|---|---|---|---|---|
| DEC-20260903-001 | 2026-09-03 | 사용자 | 확정·유지 | 백엔드만 구현 / 백엔드와 Flutter를 함께 구현 | PLC-001~PLC-023은 **백엔드까지만 구현**한다. Flutter 프론트엔드는 범위에서 제외한다. | 사용자의 구현 범위 정정 | 저장소 작업 범위, 검증 범위 | PLC-001~PLC-023 | DEC-20260903-013을 대체. Flutter 변경 복원 완료 |
| DEC-20260903-002 | 2026-09-03 | 사용자(플랜 승인) | 확정·유지 | 임시 인증 / 실제 Google 인증 경계 | Google ID Token 검증, JWT Access Token, Refresh Token 회전과 로그아웃을 실제 구현한다. | 인증 경계를 테스트 우회로 남기지 않기로 한 플랜 결정 | 백엔드 인증·보안, 사용자·기기·토큰 저장소 | AUTH-001~AUTH-009, PLC API 인증 | - |
| DEC-20260903-003 | 2026-09-03 | 사용자(플랜 승인) | 확정·유지 | 고정 데이터·가짜 클라이언트 / 실제 TourAPI와 테스트 대역 | 운영 코드는 실제 한국관광공사 TourAPI를 사용하고 테스트에서는 외부 호출 대역을 허용한다. | PLC 기준 데이터 동기화 요구와 재현 가능한 테스트를 함께 충족 | 장소 동기화 배치, 외부 API, 테스트 | PLC-001~PLC-010 | - |
| DEC-20260903-004 | 2026-09-03 | 사용자(플랜 승인) | 확정·유지 | DB 직접 증가 / Redis 누적 후 DB 반영 | 장소 조회수는 Redis에 누적하고 주기적으로 PostgreSQL에 반영한다. | 상세 조회 쓰기 부하 완화 | 조회수 집계, Redis, 장애 복구 | PLC-014 | - |
| DEC-20260903-005 | 2026-09-03 | 사용자(플랜 승인) | 확정·유지 | 최근접 지역 추정 / Google 역지오코딩 | 사용자 장소의 대한민국 범위와 시도·시군구 판정에 Google Geocoding API를 사용한다. | 행정 경계 판정 정확도 개선 | 사용자 장소 생성, 외부 API, 운영 키 | PLC-019, PLC-020 | - |
| DEC-20260903-006 | 2026-09-03 | 사용자(플랜 승인) | 확정·유지 | 기존 PlaceSummary / 대표 이미지 필드 추가 | `PlaceSummary`에 `imageUrl`을 추가한다. | 장소 목록과 상세에서 관광공사 대표 이미지를 표시할 계약 필요 | 장소 API 응답 계약 | PLC-011, PLC-012 | API 원본 명세와 변경 이력 동기화 필요 |
| DEC-20260903-007 | 2026-09-03 | 사용자(플랜 승인) | 확정·유지 | 상세와 게시글 별도 호출 / 상세에 최신 게시글 포함 | `PlaceDetail.recentPosts`로 최신 공개 게시글을 최대 12개 반환한다. | PLC-012의 한 화면 1회 API 원칙 | 장소 상세 API, 게시글 조회 | PLC-012, PLC-013 | API 원본 명세와 변경 이력 동기화 필요 |
| DEC-20260903-008 | 2026-09-03 | 사용자(플랜 승인) | 확정·유지 | 관리자 수동 판단만 / 누적 기준 자동 숨김 | 장소 신고 API를 추가하고 서로 다른 사용자 신고가 3회 누적되면 숨긴다. 관리자는 게시글을 가장 가까운 공식 장소로 재배치한다. | 신고 누적 시 숨김·재배치 요구를 구체화 | 신고 API, 장소 상태, 관리자 운영 | PLC-023 | API 원본 명세와 변경 이력 동기화 필요 |
| DEC-20260903-009 | 2026-09-03 | 에이전트 자동 결정 | 재검토 필요 | PostgreSQL 패치·digest 고정 / 메이저·PostGIS 태그만 지정 | 개발 Compose 이미지로 `postgis/postgis:16-3.4`를 선택해 PostgreSQL 패치 버전을 고정하지 않았다. | 기존 DB 엔진 결정이 PostgreSQL 16 + PostGIS 3까지만 지정 | 로컬 DB, 운영 재현성 | PLC-001~PLC-023 | 운영 반영 전 PostgreSQL 16.15 기반 이미지 digest 확정 필요 |
| DEC-20260903-010 | 2026-09-03 | 에이전트 자동 결정 | 확정·개발 전용 | JWT 키 필수 / 로컬 임시 RSA 키 허용 | 운영은 환경 변수의 RSA 키를 사용하고, 로컬에서 키가 없으면 프로세스 시작 시 임시 RSA 키를 생성한다. | 로컬 기동 편의성과 운영 키 분리 | 인증·보안, 로컬 실행 | AUTH-007~AUTH-009 | 운영 환경에서는 임시 키 사용 금지 |
| DEC-20260903-011 | 2026-09-03 | 에이전트 자동 결정 | 확정·유지 | 기존 전역 Gradle 사용 / 저장소 Wrapper 추가 | 백엔드에 Gradle Wrapper 9.5.0을 추가한다. | 동일한 빌드 도구 버전으로 테스트 재현 | 백엔드 빌드·CI | PLC-001~PLC-023 | 프로덕션 의존 버전 정책 수립 시 재검토 |
| DEC-20260903-012 | 2026-09-03 | 에이전트 자동 결정 | 확정·유지 | Redis 실패 시 조회수 유실 / DB 직접 증가 폴백 | Redis 조회수 증가가 실패하면 PostgreSQL에서 직접 1을 증가시킨다. | Redis 장애 중 조회수 유실 완화 | 조회수 장애 복구, DB 부하 | PLC-014 | DEC-20260903-004의 장애 처리 세부 결정 |
| DEC-20260903-013 | 2026-09-03 | 에이전트(범위 해석 오류) | 폐기·복원 완료 | 백엔드만 / 백엔드와 Flutter 동시 구현 | PLC 사용자 흐름을 Flutter까지 구현했으나 최종 사용자 범위와 달라 전체 Flutter 변경을 되돌렸다. | 구현 범위를 명시적으로 재확인하지 않고 확대함 | Flutter 앱, 작업 시간, 검증 환경 | PLC-001~PLC-023 | DEC-20260903-001로 대체 |
| DEC-20260903-014 | 2026-09-03 | 사용자 | 확정·유지 | 플랜 모드 결정만 대화에 유지 / 모든 작업 모드의 중요 결정을 저장소에 기록 | 플랜 모드와 일반 실행 모드 모두에서 사용자 결정과 에이전트 자동 결정을 이 문서에 기록한다. 중요한 범위·계약 변경은 사용자 확인 전 구현하지 않는다. | 세션이 달라져도 결정과 근거를 보존하고 범위 오해 재발을 방지 | 에이전트 협업, 계획, 구현 전 검토 | 협업 규칙 | 루트 `AGENTS.md`와 `docs/README.md`에 적용 |
| DEC-20260903-015 | 2026-09-03 | 사용자 | 확정·유지 | 기능 브랜치의 독립 Auth·JDBC·Flyway 구조 유지 / 최신 `upstream/develop`의 공용 Auth·JPA·Flyway 구조에 PLC 기능 통합 | 최신 `upstream/develop`의 Auth·JPA, Gradle 8.14 Wrapper와 Flyway `V1`~`V6`를 보존하고, 중복 인증·마이그레이션을 제거한 뒤 PLC 기능을 공용 인증 구조와 `V7` 이후 마이그레이션으로 통합한다. | 병합된 팀원 기능을 보존하고 중복 빈·Flyway 버전 충돌·스키마 불일치를 방지 | 백엔드 인증, 빌드 도구, 장소 모델, DB 마이그레이션, PR 충돌 해결 | PLC-001~PLC-023, AUTH-001~AUTH-009, PST-001~PST-045 | DEC-20260903-010·011을 대체. 사용자의 충돌 해결 실행 승인 |
| DEC-20260903-016 | 2026-09-03 | 사용자 | 확인 필요·미적용 | 기존 PostgreSQL 16 + PostGIS 3 / PostgreSQL 17.10.2 + PostGIS 2, JDK 21, Gradle 8.14 고정 | 백엔드 런타임을 PostgreSQL 17.10.2, PostGIS 2, JDK 21, Gradle 8.14로 고정하도록 요청했다. | 팀 개발·CI·운영 환경의 버전 재현성 확보 | 백엔드 DB 이미지·공간 확장·JDK·Gradle·문서 | PLC-001~PLC-023, 백엔드 빌드·운영 | `17.10.2`는 Percona Distribution 버전이며 PostGIS 2.x는 PostgreSQL 17을 지원하지 않아 배포판과 PostGIS 호환 조합을 사용자에게 재확인 후 적용 필요 |
| DEC-20260905-001 | 2026-09-05 | 사용자 | 확정·유지 | 기존 PostgreSQL 16 + PostGIS 3 및 JDK 21 메이저 고정 / Percona PostgreSQL 17.10.2 + PostGIS 3.5.7 + JDK 21.0.11 + Gradle 8.14 정확 버전 고정 | 백엔드 개발·테스트·운영 기준 버전을 Percona PostgreSQL 17.10.2, PostGIS 3.5.7, JDK 21.0.11, Gradle 8.14로 통일한다. JDK 배포 벤더는 지정하지 않는다. | 팀 개발·CI·운영 환경의 버전 재현성과 PostgreSQL 17·PostGIS 호환성 확보 | 백엔드 DB 이미지·통합 테스트·JDK 툴체인·Gradle Wrapper·관련 문서 | PLC-001~PLC-023, 백엔드 빌드·운영 | DEC-20260903-009·016을 대체하고 DEC-20260903-015의 Gradle 8.14 결정을 재확인 |
| DEC-20260905-002 | 2026-09-05 | 에이전트 자동 결정 | 확정·유지 | 최신 `develop`의 `V7`~`V10` 재번호 부여 / 장소 마이그레이션을 다음 빈 버전으로 이동 | 최신 `upstream/develop`의 기존 Flyway `V7`~`V10`을 보존하고 장소 확장 마이그레이션을 `V7__place_features.sql`에서 `V11__place_features.sql`로 변경한다. | 동일한 Flyway 버전 `V7` 중복으로 인한 애플리케이션 기동 실패를 방지하고, 이미 공유된 팀 마이그레이션 이력을 변경하지 않기 위함 | DB 마이그레이션 순서, 장소 스키마 배포 | PLC-001~PLC-023, PST-040~PST-045, CMU-012~CMU-031 | DEC-20260903-015의 최신 `develop` 통합 원칙을 적용 |
| DEC-20260905-003 | 2026-09-05 | 사용자 | 확정·유지 | MAP 백엔드만 구현 / 백엔드와 Flutter 동시 구현 | MAP-001~MAP-030 작업은 백엔드 API·집계·배치까지만 구현한다. Flutter 지도 화면·탭·카메라·디바운스·화면 이동·사진 회전은 제외하며 후속 Flutter 구현에는 Google Maps를 사용한다. | 팀 작업 범위를 분리하고 현재 요청의 완료 기준을 백엔드로 한정 | MAP API, 집계 배치, Flutter 후속 작업 | MAP-001~MAP-030 | Flutter 전용 요구사항은 서버 데이터 계약만 제공 |
| DEC-20260905-004 | 2026-09-05 | 사용자 | 확정·유지 | 줌·폴백·응답 제한을 미정으로 유지 / 고정 경계와 절단 정책 적용 | 줌 `0~6`은 1도, `7~9`는 0.1도, `10~13`은 0.01도, `14~22`는 0.001도 격자를 사용한다. 요청 화면의 LAST_1H 게시글이 5건 미만이면 LAST_24H로 폴백한다. 조회 셀이 500개를 넘으면 게시글 수 상위 500개를 반환하고 `truncated=true`로 표시한다. | 클라이언트가 예측 가능한 격자와 폴백·대용량 응답 정책을 갖도록 함 | 히트맵 조회 계약, 집계 단위, 응답 크기 | MAP-009~MAP-014, MAP-018 | 일반적인 셀 초과에는 `MAP_TOO_MANY_CELLS`를 반환하지 않음 |
| DEC-20260905-005 | 2026-09-05 | 사용자 | 확정·유지 | 후보 선정·썸네일 저장·즉시 반영 방식 미정 / 랭킹 우선 후보와 커밋 후 갱신 | 사진 후보는 `post_rankings.score`를 우선하고 `created_at`, `post_id`로 순서를 고정한다. `sample_post_ids`와 `sample_thumbnail_urls`를 같은 순서의 배열로 저장한다. 게시글 생성 커밋 후 영향을 받는 네 격자와 지역 통계를 비동기로 다시 계산하며 `forceRefresh`는 응답 캐시만 우회한다. | 마커 조회 조인을 줄이고 업로드 직후 반영과 재현 가능한 후보 순서를 보장 | 히트맵 저장 구조, 이벤트 처리, 마커 API | MAP-015, MAP-020~MAP-025 | - |
| DEC-20260905-006 | 2026-09-05 | 사용자 | 확정·유지 | V11 별도 핫픽스·기존 명세 유지·주변 장소 페이징 추가 / 한 MR에서 마이그레이션과 계약 보정 | MAP MR에서 V11의 `bookmarks`·`reports` 중복 생성을 제거한다. v1.1.5 원본 명세에 썸네일 URL 배열, `nearestDistanceM`, 기간을 포함한 불투명 `cellKey`를 반영한다. API-PLC-004의 불완전한 `cursor`·`size` 항목은 제거하고 거리순 최대 50개 계약을 유지한다. | 신규 DB 마이그레이션 실패와 구현·명세의 불일치를 같은 변경에서 해소 | Flyway V11, MAP·PLC API, 명세 문서 | MAP-017, MAP-025~MAP-030, PLC-004 | DEC-20260905-002의 V11에 대한 후속 정정 |
| DEC-20260905-007 | 2026-09-05 | 사용자 | 확정·임시 | MAP 범위를 방문 기록까지 확대 / 방문 기능 전까지 0 제공 | 기간별 방문 기록 원천이 구현되기 전까지 `HeatmapCell.visitCount`는 계약 필드를 유지하되 0을 반환한다. | 장소 누적 방문 수는 요청 기간과 의미가 달라 잘못된 값을 만들며 VST 구현은 현재 범위를 크게 확장함 | 히트맵 응답, 향후 방문 집계 | MAP-017, VST-001~VST-010 | VST 방문 저장 구현 시 실제 기간 집계로 대체 |
| DEC-20260905-008 | 2026-09-05 | 에이전트 자동 결정 | 확정·유지 | 매 요청 실시간 집계 / PostgreSQL 스냅샷 + Redis 버전 캐시 | PostgreSQL 집계 테이블을 원본으로 사용하고 지도 응답은 Redis에 최대 60초 캐시한다. 게시글 반영 시 전역 캐시 버전을 증가시키며 Redis 장애 시 DB를 직접 읽는다. 전체·증분 집계는 PostgreSQL advisory transaction lock으로 직렬화한다. | MAP-008의 사전 집계 원칙을 지키고 다중 인스턴스의 중복 배치·캐시 무효화 경합을 낮추기 위함. 대안인 JVM 로컬 캐시는 인스턴스 간 일관성이 없어 제외 | 집계 데이터 흐름, Redis 장애 복구, 배치 동시성 | MAP-008, MAP-012, MAP-015 | 기존 Redis 운영 의존성을 재사용하며 신규 외부 의존성은 없음 |
| DEC-20260905-009 | 2026-09-05 | 사용자 | 확정·유지 | 현재 Flyway 스키마 변경 / 제공된 v1.1.3 ERD를 독립 SQL로 제공 | 제공된 ERD의 실제 28개 테이블을 Percona PostgreSQL 17.10.2 + PostGIS 3.5.7용 독립 초기화 SQL과 소량의 관계형 더미 데이터 SQL로 작성한다. 기존 UUID 기반 Spring Boot·Flyway 스키마에는 연결하거나 적용하지 않는다. | 제공 ERD는 bigint 사용자 ID와 PostgreSQL enum을 사용해 현재 애플리케이션 스키마와 호환되지 않으므로, pgAdmin에서 별도 빈 데이터베이스에 실행할 수 있는 산출물로 격리한다. | 독립 DB 초기화·샘플 데이터, 기존 애플리케이션 비영향 | 데이터 설계 v1.1.3 참조본 | 표기상 29개이나 실제 정의된 테이블은 28개. DB 계정 비밀번호는 SQL에 포함하지 않음 |
| DEC-20260905-010 | 2026-09-05 | 에이전트 자동 결정 | 확정·유지 | PostGIS 이미지 `17.10-2` 유지 / 실제 버전이 일치하는 `17.10-5`로 교정 | Compose·Testcontainers·실행 문서의 이미지를 `percona/percona-distribution-postgresql-with-postgis:17.10-5`로 통일한다. 검증한 멀티아키텍처 이미지 digest는 `sha256:3a162e3e590350fb7be8af7210785702ff4874abcd67977c5ee32f45d4d08e1e`다. | `17.10-2` 이미지는 실제로 Percona Server 17.10.1·PostGIS 3.5.6을 제공했으며, `17.10-5`에서 사용자 고정 버전인 Percona Server 17.10.2·PostGIS 3.5.7을 확인했다. | 로컬 Compose, 통합 테스트, DB 실행 문서 | 백엔드 DB·공간 확장 버전 고정 | DEC-20260905-001의 버전 결정은 유지하고 잘못된 Docker 태그 매핑만 대체 |
| DEC-20260905-011 | 2026-09-05 | 사용자 | 확정·유지 | 기존 구현 중심 29테이블 문서를 유지 / 사용자가 제공한 ERD의 실제 28테이블을 최신 정본으로 채택 | 데이터 설계 문서의 기준을 사용자가 제공한 ERD로 통일한다. 제공 ERD에 없는 `tier_logs`는 의도적으로 빠진 최신 상태이므로 정본 테이블 수는 28개이며, `heatmap_refresh_state` 같은 구현 보조 테이블도 정본 ERD에 포함하지 않는다. 이번 작업은 문서 정합화까지만 수행하고 Flyway·애플리케이션 스키마와 RNK 구현은 변경하지 않는다. | 사용자가 “테이블 하나 빠진 게 최신”이라고 명시했으며, 구현 스키마까지 동시에 변경하면 기존 코드·마이그레이션과의 호환성 판단이 추가로 필요하다. | `docs/04-data-design.md`, `docs/05-erd-reference.md`, `docs/12-db-schema.dbml`, `docs/08-spec-changelog.md`. 구현과 문서 사이 차이는 후속 마이그레이션 결정 전까지 명시적으로 유지한다. | 데이터 설계, RNK-001~013 구현 선행조건 | DEC-20260905-009의 독립 실행 SQL을 문서 정본 기준으로 확장. RNK 구현 보류는 유지한다. |
| DEC-20260905-012 | 2026-09-05 | 에이전트 자동 결정 | 확정·유지 | 데이터 설계 3개 문서만 수정 / 원본 API XLSX와 파생 문서까지 같은 ERD로 동기화 | `docs/specs/snaphere-api-spec-v1.1.5.xlsx`의 ERD 엔터티·관계 시트를 28테이블·42관계로 맞추고, `tier_logs`를 가리키던 저장소 주석을 정본 테이블과 계산값으로 교정한다. 원본 수정 뒤 파생된 `docs/03-api-spec.md`와 `docs/README.md`도 동기화한다. 엔드포인트·요청·응답 계약과 문서 버전은 바꾸지 않는다. | `docs/03`을 직접 고치지 말고 원본 스프레드시트를 먼저 수정하라는 협업 규칙을 지키면서 저장소 안의 상충 표기를 남기지 않기 위함이다. 대안인 04·05·12만 수정하면 API 원본 ERD가 계속 29테이블을 주장해 정본이 둘로 갈린다. | API 명세 원본의 ERD 시트·저장소 주석, `docs/03-api-spec.md`, `docs/README.md`; 애플리케이션 코드·DB 무영향 | API-PST-002·003·006, 데이터 설계 | DEC-20260905-011 적용 범위를 파생 문서까지 명확화한다. |
| DEC-20260905-017 | 2026-09-05 | 사용자 | 확정·유지 | RNK 구현 보류 유지 / 앞서 정리한 RNK-001~RNK-013 백엔드 계획 실행 | RNK-001~RNK-013의 장소 점수 집계, 기간·지역·전국·테마·장소 유형별 랭킹 조회, 이전 순위, 추천 사유 코드와 무작위 요소, 데이터 부족 시 운영자 지정 장소 폴백을 구현한다. 운영자 지정 여부는 정본 ERD의 미결정 제안이었던 `places.is_curated`를 활성화하여 저장하고, 구현은 기존 문서 PR과 분리된 기능 브랜치에서 진행한다. Flutter 화면 구현은 포함하지 않는다. | 사용자가 보류했던 RNK 구현을 다시 명시적으로 요청했고 RNK-013을 포함한 전체 범위를 승인했다. `places.is_curated`는 정본 문서가 이미 제안한 최소 필드여서 별도 운영 테이블보다 변경 범위가 작다. | Flyway 애플리케이션 스키마, 랭킹·추천 API, 집계 스케줄러와 수동 배치, 데이터 설계 문서 | RNK-001~RNK-013, API-RNK-001~002, JOB-008 | DEC-20260905-011의 RNK 구현 보류를 대체한다. 독립 초기화 SQL은 별도 산출물 원칙을 유지한다. 원래 ID `DEC-20260905-013`은 최신 develop과 충돌하여 병합 전에 변경했다. |
| DEC-20260905-018 | 2026-09-05 | 에이전트 자동 결정 | 확정·유지 | 조회 시 순위 재계산 / 집계 테이블에 범위·장소유형 차원을 추가하고 최소 진입 기준을 적용 | `place_rankings`에 `scope=NATIONAL\|REGION`과 `place_type=ALL\|OFFICIAL\|USER` 차원을 추가하여 각 목록의 촘촘한 순위를 배치에서 미리 계산한다. 랭킹 진입 기준은 선택 기간에 활성 게시글 1개 이상으로 두고, 동점은 `place_id ASC`로 고정한다. 추천은 주간 전체 랭킹 점수에 요청별 ±10% 무작위 계수를 적용하며 좌표가 있으면 20km 이내를 대상으로 한다. | 기존 한 개의 `rank_no`로는 전국·지역 및 전체·사용자 장소 순위를 동시에 나타낼 수 없다. 조회 시 `row_number()`를 다시 계산하는 대안은 RNK-002의 사전 집계 원칙에 어긋난다. 진입 기준 수치가 명세에 없으므로 유효 데이터를 배제하지 않는 최소 조건을 선택했고, 무작위 폭은 상위 품질을 크게 훼손하지 않으면서 반복 노출을 줄이는 값이다. | Flyway `place_rankings`, JOB-008 집계 SQL, 랭킹·추천 조회 의미 | RNK-002~RNK-011 | 기준치는 후속 사용자 결정으로 조정 가능. DEC-20260905-017을 구체화한다. 원래 ID `DEC-20260905-014`는 최신 develop과 충돌하여 병합 전에 변경했다. |
| DEC-20260905-015 | 2026-09-05 | 에이전트 자동 결정 | 확정·유지 | JDBC·JPA 장소 저장소 클래스명 변경 / Spring 빈 이름만 명시적으로 분리 | JDBC 구현 `com.snaphere.api.place.PlaceRepository`의 빈 이름을 `placeJdbcRepository`로 지정한다. JPA 인터페이스 `com.snaphere.api.place.repository.PlaceRepository`의 기본 빈 이름은 유지한다. | 실제 DB 컨텍스트 검증에서 두 클래스가 모두 `placeRepository`로 등록되어 기동이 중단됐다. 클래스나 주입 타입을 바꾸는 대안보다 빈 이름만 분리하는 방식이 영향이 작고 기존 타입 기반 주입도 유지한다. | Spring 애플리케이션 기동, 장소·랭킹 저장소 주입 | RNK-001~RNK-013 검증 선행조건 | 기능 계약·DB 계약 변화 없음 |
| DEC-20260905-016 | 2026-09-05 | 에이전트 자동 결정 | 확정·유지 | 중복 태그 추천 컨트롤러 유지 / 정본 API-CMU-011 구현만 유지 | `GET /api/v1/tags/suggestions`는 장소 전용 구형 메서드를 `PlaceController`에서 제거하고, 인증·행사 고정 태그·지역·카테고리 추천을 모두 지원하는 `TagController` 구현만 매핑한다. | 두 컨트롤러가 같은 경로를 매핑해 애플리케이션 기동이 중단됐다. API 명세는 API-CMU-011 한 개만 정의하며 `TagController`가 그 요청 파라미터와 인증 계약을 충족한다. | Spring MVC 기동, 태그 추천 엔드포인트 | API-CMU-011, PLC-021, CMU-025~029 | API 계약 변화 없음. RNK 실제 DB 검증 중 발견한 기존 충돌 해결 |
| DEC-20260906-029 | 2026-09-06 | 사용자 | 확정·유지 | 전체 검증 결함 수정 / 사용자 담당 구현 영역만 수정 | 현재 검증 결함 중 사용자가 구현을 진행한 `PLC·MAP·RNK` 범위만 수정한다. 이번 결함 목록에서는 장소 게시글 조회를 공유하는 장소 API 3건과 랭킹 필수 파라미터 오류 1건을 대상으로 하고, `AUTH·PST·CMU`의 인증·좋아요·북마크 결함은 팀원 영역으로 남겨 수정하지 않는다. | 구역별 담당 코드에 한정해 변경하고 팀원의 미완료 작업과 충돌하지 않으려는 사용자 요청 | 장소 조회 SQL, 장소 관리자 응답, 랭킹 요청 검증, 회귀 테스트 | PLC-012~013, PLC-022, PST-027, RNK-002~003, SYS-001~002 | API·DB 계약 변경 없이 기존 계약대로 오류만 교정. 원래 ID `DEC-20260906-001`은 최신 develop과 충돌하여 병합 전에 변경했다. |
| DEC-20260906-030 | 2026-09-06 | 사용자 | 확정·유지 | 충돌 가능성이 있는 변경을 임의 해결하지 않음 / 사전 제시한 통합 원칙대로 최신 `upstream/develop` 병합 | 현재 RNK·PLC 변경을 로컬 커밋으로 보존한 뒤 최신 `upstream/develop`을 병합한다. `PlaceController`는 최신 브랜치의 최근 본 장소 엔드포인트와 컨트롤러 소유권 정리를 유지하고, `PlaceRepository`는 양쪽 조회 기능과 `aspect_ratio` 별칭 수정 모두를 보존한다. 설정은 양쪽 배치 항목을 합치며, 결정 기록은 최신 브랜치 ID를 유지하고 로컬 중복 ID를 변경한다. 로컬 장소 랭킹 Flyway 파일은 최신 브랜치의 V13~V18과 겹치지 않도록 V19로 변경한다. | 사용자가 충돌 확인 후 해결 전 승인을 요구했고, 보고된 텍스트 충돌 4건과 Flyway 버전 충돌의 구체적인 해결안을 승인했다. | Git 병합, 장소 API·저장소, 배치 설정, 결정 기록, Flyway 마이그레이션 순서 | PLC, MAP, RNK-001~RNK-013, DB 마이그레이션 | 원격 푸시는 별도 요청 전까지 수행하지 않는다. |
| DEC-20260906-001 | 2026-09-06 | 사용자 | 확정·진행 중 | 최신 ERD의 bigint 사용자 ID로 전체 전환 / 기존 UUID 사용자 ID 유지 후 USER·SOC 도메인 확장 | USER와 SOC는 기존 애플리케이션의 `users.id` UUID를 유지한다. 최신 ERD의 USER 필드·탈퇴 정책은 UUID 기반 Flyway 확장으로 반영하며, SOC API는 USER 기반 구현 뒤 별도 단계에서 추가한다. | 기존 인증·JWT·게시글·댓글·좋아요·저장 테이블이 UUID FK를 사용한다. bigint 전환은 전 도메인 식별자 마이그레이션이므로 USER 기능 범위를 넘어선다. USER 탈퇴·프로필이 필요한 `follows` 관계는 SOC 단계에서 추가한다. | USER·SOC 스키마와 API 구현 순서, 인증·게시글 연계 | USER-001~023, SOC-001~014, AUTH-001~014 | DEC-20260905-011의 문서 정본과 애플리케이션 스키마 분리 원칙을 USER·SOC 구현에 적용. 구현 순서: USER 기반 → SOC → USER API |
| DEC-20260906-002 | 2026-09-06 | 사용자 | 확정·진행 중 | 팔로우 카운터 보정 배치 시각 미정 / 매일 05:10 KST 실행 | 팔로우 관계의 실제 COUNT로 `follower_count`·`following_count`를 재계산하는 SOC-008 배치를 매일 05:10 KST에 실행한다. | 계정 파기 배치(05:00)와 실행 시점을 분리하면서 요구사항의 “매일 새벽” 조건을 충족한다. | SOC 배치 운영, 사용자 통계 정합 | SOC-008 | 기본 cron은 환경 변수로 변경 가능 |
| DEC-20260905-013 | 2026-09-05 | 사용자 | 확정·유지 | JdbcClient 장소 리포지터리 클래스명 변경 / 빈 이름만 `placeJdbcRepository` 로 지정 / `allow-bean-definition-overriding` 허용 | `com.snaphere.api.place.PlaceRepository`(JdbcClient)에 `@Repository("placeJdbcRepository")` 를 붙여 빈 이름을 못 박는다. 같은 단순명의 JPA 인터페이스 `com.snaphere.api.place.repository.PlaceRepository` 는 그대로 `placeRepository` 로 등록된다. | 두 클래스가 같은 기본 빈 이름 `placeRepository` 를 만들어 덮어쓰기 금지 상태에서 애플리케이션이 기동하지 못했다(2026-09-05 로컬 기동 실패). 둘 다 살아 있는 코드라 삭제할 수 없고, 파일을 옮기면 병합 중인 브랜치가 전부 충돌한다. 덮어쓰기 허용은 어느 빈이 이기는지 불명확해 제외 | 장소 도메인 빈 등록, 애플리케이션 기동 | PLC-001~PLC-023, MAP-026, PST-002 | 주입은 전부 타입 기준이라 사용처 변경 없음 |
| DEC-20260905-014 | 2026-09-05 | 사용자 | 확정·유지 | `TagController#suggestions` 제거 / `PlaceController#tags` 제거 / 매핑만 떼고 메서드 유지 | `GET /api/v1/tags/suggestions` 중복 매핑 중 `PlaceController#tags` 와 `PlaceService.tags` 를 제거하고 `TagController#suggestions` 하나만 남긴다. `PlaceRepository.tagSuggestions`·`placeTag` 는 당장 지우지 않고 남겨 둔다. | 두 컨트롤러가 같은 경로를 물어 `Ambiguous mapping` 으로 애플리케이션이 기동하지 못했다(2026-09-05 로컬 기동 실패). 명세 API-CMU-011 은 이 엔드포인트 하나가 장소 태그(PLC-021)·행사 고정 태그(EVT-017~020)·타이핑 접두어(CMU-026~028)를 모두 처리하도록 정의하는데, `PlaceController` 구현은 `placeId` 만 받아 `eventId` 를 다루지 못해 명세를 만족하지 못한다 | 태그 추천 API, 장소 도메인 | PLC-021, CMU-025~029, EVT-017~020 | DEC-20260905-013 과 같은 develop 병합 잔재. 두 건이 잔재 전부임을 정적 스캔으로 확인 |
| DEC-20260906-020 | 2026-09-06 | 에이전트 자동 결정 | 확정·유지 | 커서 시각을 밀리초로 유지하고 조회 조건을 범위 비교로 완화 / 커서에 초·나노초를 담아 정밀도를 맞춤 | 커서에 시각을 담는 네 곳(`PostCursor`·`CommentCursor`·`FollowCursor`·`NotificationCursor`)을 `epochSecond:nano:id` 형식으로 바꾼다. 두 토막짜리 옛 커서는 밀리초로 해석해 계속 받는다. | PostgreSQL `timestamptz` 는 마이크로초까지 보관하는데 `toEpochMilli()` 가 그 아래를 버린다. 커서 시각이 실제 값보다 앞서므로 조회 조건의 `createdAt = :cursor` 가 영영 맞지 않고, `createdAt < :cursor` 도 같은 밀리초의 행을 걸러 낸다 — 다음 페이지가 비어 돌아온다(2026-09-06 로컬 검증, 알림 2페이지 유실). 조회 조건을 `<=` 로 완화하면 경계 행이 중복돼 무한 스크롤에 같은 카드가 두 번 뜬다 | 게시글 피드·댓글·팔로우 목록·알림함 커서 페이징 | CMU-010, CMU-013, SOC-005, SOC-006, NTF-011, SYS-003, SYS-004 | 응답 계약과 DB 스키마는 바뀌지 않는다. 커서는 불투명 문자열이라 앱 변경도 필요 없다 |
| DEC-20260906-015 | 2026-09-06 | 에이전트 자동 결정 | 확정·유지 | upstream 의 `V13`·`V14` 를 밀어내고 기존 번호 유지 / 내 마이그레이션을 다음 빈 번호로 이동 | `upstream/develop` 이 `V13__user_foundation.sql`·`V14__follow_schema.sql` 을 먼저 차지했으므로 이쪽 마이그레이션을 뒤로 옮긴다 — 방문 `V13`→`V15`, 이벤트 인덱스 `V14`→`V16`, 뱃지 `V15`→`V17`. | 같은 버전 번호가 둘이면 Flyway 가 `Found more than one migration with version` 으로 기동을 거부한다. 이미 공유된 팀 마이그레이션 이력은 바꾸지 않는다는 원칙을 그대로 적용했다. 번호는 머지 예정 순서(방문 → 이벤트 → 뱃지)와 같게 매겨 out-of-order 적용을 피한다 | DB 마이그레이션 순서, 로컬 DB 초기화 | VST-001~010, EVT-005~010, BDG-009~014 | DEC-20260905-002 와 같은 상황의 반복 |
| DEC-20260906-016 | 2026-09-06 | 에이전트 자동 결정 | 확정·유지 | 팀 결정 ID 를 밀어내고 내 번호 유지 / 내 결정을 뒤 번호로 이동 | 같은 날 팀이 먼저 쓴 `DEC-20260906-001`·`-002` 를 그대로 두고, 이쪽에서 같은 번호로 기록했던 두 건을 `DEC-20260906-013`·`-014` 로 옮긴다. 커밋 메시지에 남은 옛 ID 는 고치지 않는다. | 같은 날 서로 다른 브랜치에서 작업하면 ID 가 겹친다. 먼저 develop 에 들어간 쪽을 기준으로 삼는 편이 이력 추적에 낫고, 과거 커밋 메시지를 고치면 사실이 덮인다(AGENTS.md 기록 규칙 5) | 의사결정 이력 | - | 옛 ID: 시도별 요약 JPQL 전환=`001`→`013`, 참여 게시글 리포지터리 분리=`002`→`014` |
| DEC-20260906-013 | 2026-09-06 | 에이전트 자동 결정 | 확정·유지 | 네이티브 쿼리 + 인터페이스 프로젝션 유지하며 별칭 보정 / JPQL 로 전환 | 시도별 이벤트 요약(`API-EVT-006`) 쿼리를 네이티브에서 JPQL 로 바꾼다. PostgreSQL 전용 `count(...) filter (where ...)` 는 표준 `sum(case when ... then 1 else 0 end)` 으로 대체하고, 집계가 null 을 줄 수 있는 `newCount` 는 래퍼 타입으로 받아 서비스에서 0 으로 메운다. | 네이티브 쿼리의 인터페이스 프로젝션은 결과셋 별칭을 대소문자까지 그대로 찾는데 PostgreSQL 은 따옴표 없는 식별자를 소문자로 접는다. 큰따옴표를 붙여도 맞지 않아 `COMMON_500` 이 났다(2026-09-06 로컬 검증). JPQL 은 별칭 대신 프로퍼티 이름으로 맞추므로 이 층이 사라지고, `filter` 절을 표준 구문으로 바꾸면 H2 테스트 환경과도 어긋나지 않는다 | 이벤트 시도별 요약 조회 | EVT-007, EVT-008, EVT-009 | 이벤트 목록(`API-EVT-001`)은 정렬 그룹 파생 컬럼이 필요해 네이티브를 유지한다 |
| DEC-20260906-003 | 2026-09-06 | 에이전트 자동 결정 | 확정·유지 | `events.fixed_tags` 를 문자열로 읽어 애플리케이션에서 파싱 / `@JdbcTypeCode(SqlTypes.JSON)` 으로 `List<String>` 직접 매핑 | Hibernate 6 의 `@JdbcTypeCode(SqlTypes.JSON)` 으로 jsonb 열을 `List<String>` 에 직접 매핑한다. 응답 변환은 `EventFixedTags` 가 맡고 `locked = true`, `tagId = null` 로 내보낸다. | 별도 라이브러리(hypersistence-utils 등)를 프로덕션 의존성에 더하지 않으려는 것이다. `tagId` 를 비우는 이유는 `fixed_tags` 가 표시용 이름만 담기 때문이다 — tagId 를 저장하면 태그가 병합·삭제될 때 이 열이 낡는다 | 이벤트 엔터티, 고정 태그 응답 | EVT-017, EVT-018, EVT-019 | 실제 태그 행 연결은 게시글 생성 시점에 태그 도메인이 한다 |
| DEC-20260906-004 | 2026-09-06 | 에이전트 자동 결정 | 확정·유지 | 숨긴 행사에 403 / 404 반환 | 숨긴 행사(`events.status = HIDDEN`)는 상세·참여 게시글 모두 `EVENT_NOT_FOUND`(404) 로 막는다. 행사장이 숨겨진 경우도 상세는 404 다. | 403 은 '있긴 한데 못 본다' 를 뜻해 존재 자체가 새어 나간다. 목록에서도 빠지므로 앱이 보는 세계와 일치시킨다. 참여 게시글을 빈 목록 200 으로 돌리지 않는 것도 같은 이유다 — 상세가 404 인데 목록만 200 이면 앱이 두 응답을 다르게 읽는다 | 이벤트 상세·참여 게시글 조회 | EVT-011, EVT-014 | - |
| DEC-20260906-014 | 2026-09-06 | 에이전트 자동 결정 | 확정·유지 | `PostRepository.findFeed` 에 `eventId` 파라미터 추가 / 별도 `findEventPosts` 메서드 | 행사 참여 게시글(`API-EVT-004`)은 `findFeed` 를 확장하지 않고 `findEventPosts` 를 새로 둔다. | `findFeed` 는 피드·태그·장소 세 서비스가 쓰고 테스트도 그 시그니처에 묶여 있어 파라미터를 하나 늘리면 이 슬라이스와 무관한 코드 다섯 곳이 함께 바뀐다. 참여 게시글의 필터는 `eventId` 하나뿐이라 커서 조건을 복사해도 갈라질 여지가 작다 | 게시글 조회 리포지터리 | EVT-014, PST-034 | `findFeed` 의 javadoc 이 경고하는 '조합마다 메서드 분리' 와는 다른 축이다 |
| DEC-20260906-005 | 2026-09-06 | 사용자 | 확정·유지 | `NoOpEventFixedTagReader` 조건부 등록으로 유지 / 삭제 | `events` 테이블을 읽는 `JpaEventFixedTagReader` 를 추가하면서 `NoOpEventFixedTagReader` 를 삭제한다. | 두 구현이 모두 무조건 등록돼 있어 함께 두면 중복 빈으로 애플리케이션이 뜨지 않는다. `StubEventData` 와 달리 이 클래스는 `stub-data` 축과 무관해 조건을 걸 근거가 없고, 파일 자신의 javadoc 도 '실제 구현을 추가할 때 이 파일을 지운다' 고 적고 있다. git 이력에 남아 복구 가능하다 | 태그 추천, 게시글 등록의 고정 태그 | EVT-017, EVT-018, CMU-028 | DEC-20260905-011 과 달리 삭제를 택한 이유를 함께 기록한다 |
| DEC-20260906-006 | 2026-09-06 | 에이전트 자동 결정 | 확정·유지 | 자유 태그 초과를 조용히 자르기 / 422 로 거절 | 행사 참여 업로드에서 자유 태그가 `10 - 고정 개수` 를 넘으면 `POST_TAG_REQUIRED`(422) 로 거절한다. 고정 태그는 서버가 다시 주입하고 `locked = true` 로 저장하며, 정규화 이름으로 중복을 지울 때 고정 쪽을 남긴다. | 페이지 크기처럼 조용히 자르는 값과 달리 태그는 사용자가 직접 넣은 것이다. 말없이 사라지면 게시 결과가 입력과 달라진다. 잠금 비교를 정규화 이름으로 하는 이유는 사용자가 ' 서울 ' 을 쳐도 같은 태그 행으로 합쳐지기 때문이다 — 표시 이름으로 비교하면 그 태그가 잠기지 않는다 | 게시글 등록 검증, 태그 저장 | EVT-017~EVT-020, PST-004 | 자유 태그 칸 수는 `EventUploadContext.freeTagSlots` 로 앱에 내려 준다 |
| DEC-20260906-007 | 2026-09-06 | 에이전트 자동 결정 | 확정·유지 | 뱃지 대상 글만 참여로 세기 / 등급과 무관하게 세기 | `events.participant_count` 는 등급을 보지 않고 행사에 연결된 모든 공개 게시글을 센다. 반경 밖에서 올려 `LOW` 로 떨어진 글도 참여로 센다. | 참여 게시글 목록(EVT-014)이 등급을 가리지 않으므로 카운터만 가리면 화면의 개수와 숫자가 어긋난다. 뱃지를 못 받는 것(EVT-023)과 참여로 세지 않는 것은 다른 이야기다 | 이벤트 참여 카운터, 행사 상세 | EVT-014, EVT-021, EVT-023 | 카운터 갱신은 게시글 생성 트랜잭션 안에서 돈다 |
| DEC-20260906-008 | 2026-09-06 | 에이전트 자동 결정 | 확정·유지 | `BadgeSummary` 를 `nameKey` 로 유지 / 명세대로 `name`·`description` 을 서버가 채움 | `BadgeSummaryResponse` 를 명세 형태로 맞춘다 — `badgeId`(bdg_ 외부 ID)·`type`·`name`·`description`·`iconUrl`·`isObtainable`·`earned`·`earnedAt`. 이름은 `badges.name_ko`·`name_en` 중 `Accept-Language` 로 고른다. | SYS-010('서버는 완성 문장을 만들지 않는다')은 '좋아요 3개가 달렸어요' 같은 조립 문장을 두고 한 말이다. 뱃지 이름은 운영이 입력한 고정 문자열이고 테이블이 두 열로 갖고 있으며, 명세도 `name` 을 locale 별 이름으로 정의한다. 기존 DTO 의 javadoc 도 'BDG 조회 API 가 들어오면 그쪽 응답과 맞춘다' 고 적고 있었다 | 게시글 등록 응답, 행사 상세·업로드 컨텍스트, 뱃지 수집함 | BDG-009~BDG-013, EVT-011, PST-016, SYS-010 | `AwardedBadge` 도 같은 형태로 바꿨다 |
| DEC-20260906-009 | 2026-09-06 | 에이전트 자동 결정 | 확정·유지 | 완주 뱃지 진행값을 `visits` 로 계산 / `posts` 로 계산 | 완주 뱃지(`VISITED_AREA_COUNT`)의 진행값을 게시글의 서로 다른 `area_code` 수로 센다. 낮음 등급 게시글은 제외한다. | `visits` 테이블은 VST 브랜치에 있고 이 브랜치(develop 기준)에는 없다. 방문 기록 자체가 '높음·보통 게시글을 올리면 남는' 것이라 같은 집합이다(VST-001). 다만 같은 장소를 여러 번 올려도 방문은 하루 한 번이므로, VST 가 develop 에 들어오면 그쪽으로 옮기는 편이 정확하다 — 쿼리 javadoc 에 그 사실을 적어 두었다 | 뱃지 조건 평가 | BDG-003, BDG-007, VST-001, PST-026 | VST 병합 후 재검토 필요 |
| DEC-20260906-010 | 2026-09-06 | 에이전트 자동 결정 | 확정·유지 | `NoOpEventBadgeReader` 삭제 / `JpaEventBadgeReader` 를 `@Primary` 로 등록 | 실제 구현을 `@Primary` 로 올리고 NoOp 구현은 지우지 않는다. | 이벤트 도메인이 뱃지 스키마 없이도 컴파일·기동되게 했던 장치라 남겨 두는 편이 안전하다. `@Primary` 면 중복 빈 오류도 나지 않는다. `JpaTierDecisionLogger`·`Slf4jTierDecisionLogger` 가 이미 같은 관계다 | 행사 뱃지 미리보기 | BDG-001, EVT-011 | DEC-20260906-005(고정 태그 NoOp 삭제)와 달리 여기서는 남긴다 — 저쪽은 두 구현이 모두 무조건 등록돼 충돌했다 |
| DEC-20260906-011 | 2026-09-06 | 에이전트 자동 결정 | 확정·유지 | 뱃지 지급을 별도 스레드로 비동기 처리 / 게시글 트랜잭션 안에서 동기 처리 | 뱃지 지급 판정을 게시글 생성 트랜잭션 안에서 수행한다. BDG-005 의 '비동기 지급' 은 게시글 저장을 막지 않는다는 뜻으로 읽는다. | 명세 API-PST-003 의 응답이 `earnedBadges` 를 담으므로 별도 스레드로 빼면 응답에 실을 수 없다. 판정에 드는 것은 카운트 쿼리 몇 번이고 임계값이 다른 뱃지끼리 집계를 공유한다. 트랜잭션 안이라 게시글이 롤백되면 뱃지도 함께 되돌아간다 — 비동기면 실패한 업로드가 뱃지만 남길 수 있다 | 게시글 등록, 뱃지 지급 | BDG-005, PST-016 | 획득 알림(BDG-008)은 NTF 슬라이스에서 커밋 후 발송으로 붙인다 |
| DEC-20260906-012 | 2026-09-06 | 에이전트 자동 결정 | 확정·유지 | `save()` 로 획득 기록 저장 / `insert ... on conflict do nothing` | 뱃지 지급을 네이티브 `insert ... on conflict do nothing` 으로 하고 영향 행 수로 새 지급 여부를 판정한다. | Spring Data 의 `save()` 는 `@EmbeddedId` 가 채워져 있으면 기존 행으로 보고 merge 를 시도해 이미 받은 뱃지의 `earned_at` 을 덮는다 — 획득 시각이 사라진다. 후보 조회로 이미 받은 뱃지를 걸러도 같은 사용자의 동시 요청은 둘 다 통과하므로, 중복 방지는 PK 에 맡겨야 한다(BDG-006) | 뱃지 지급 | BDG-005, BDG-006 | PostgreSQL 전용 구문이다 |
| DEC-20260906-026 | 2026-09-06 | 사용자 | 확정·유지 | `NoOpVisitRecorder` 인자를 맞추고 조건부로 남김 / 삭제 | `NoOpVisitRecorder` 를 삭제한다. `JpaVisitRecorder` 가 실제 구현이고 조건 없이 등록된다. | 인터페이스가 `postId` 를 받는 5인자로 바뀌었는데 NoOp 이 4인자로 남아 `develop` 이 컴파일되지 않았다. 인자만 맞춰도 두 구현이 모두 무조건 등록돼 중복 빈으로 기동이 막힌다. 파일 자신의 javadoc 이 '실제 구현을 추가할 때 이 파일을 지운다' 고 적고 있고, `NoOpEventFixedTagReader`(DEC-20260906-005)를 같은 이유로 지운 선례가 있다. `@Primary` 로 가르는 쪽(DEC-20260906-010)은 뱃지처럼 스키마 없이도 상위 도메인을 띄워야 하는 경우에만 쓴다 — 방문은 `V15` 가 이미 `develop` 에 있어 그 경로가 필요 없다 | 방문 자동 기록, 게시글 등록 | VST-001, VST-002 | git 이력에 남아 복구 가능하다. 이 결함이 머지까지 간 이유는 브랜치를 합친 결과를 컴파일하지 않고 머지 충돌 여부만 확인했기 때문이다 |
| DEC-20260906-027 | 2026-09-06 | 사용자 | 확정·유지 | 탈퇴 시 게시글 일괄 삭제 / 사용자별 게시글 처리 선택 | `POST /api/v1/me/deletion`의 `contentAction`으로 사용자가 `KEEP_ANONYMIZED` 또는 `DELETE_ALL`을 선택한다. 전자는 유예 종료 시 게시글 작성자를 익명 시스템 계정으로 이전하고, 후자는 게시글을 논리 삭제한다. | 게시글 보존 여부는 개인정보·콘텐츠 정책에 직접 영향을 주므로 서버가 단일 방식을 강제하지 않고, 명세의 USER-016 선택지를 실제 API·배치로 구현한다. | 탈퇴 API, 사용자 파기 배치, posts/users 관계 | USER-015~019, USER-016, JOB-010 | 익명 시스템 계정은 로그인·프로필 조회 대상이 아니다. |
| DEC-20260906-028 | 2026-09-06 | 사용자 | 확정·유지 | 탈퇴 즉시 개인정보 파기 / 30일 유예 중 계정 정보 보존 및 사용자 선택 복구 | 탈퇴 요청 후 30일 동안 계정 정보와 콘텐츠를 유지한다. 같은 Google 계정 로그인 시 복구를 제안하고, 사용자가 복구를 선택하면 기존 계정을 ACTIVE로 되돌린다. 유예 종료 때만 선택한 콘텐츠 처리와 계정 물리 삭제를 수행한다. | 사용자가 유예 기간 내 취소·복구할 수 있도록 명시했다. | 탈퇴·복구, 개인정보 보존 기간, 계정 파기 배치 | USER-017, USER-019~021, API-AUTH-001, API-AUTH-006 | DEC-20260906-027의 콘텐츠 처리 선택은 유지한다. |
| DEC-20260906-029 | 2026-09-06 | 사용자 | 확정·로컬 개발 | 기존 기본 DB `snaphere` 사용 / 로컬 DB `snaphere_demo` 사용 | 로컬 Spring Boot 실행은 `localhost:5432/snaphere_demo`에 `postgres` 계정으로 연결한다. 접속 비밀번호는 Git에 추적되지 않는 루트 `.env`에서만 관리하고 문서·공용 설정에는 기록하지 않는다. | 사용자가 생성한 로컬 DB와 제공한 후보 계정 중 실제 접속에 성공한 계정을 사용한다. 공유 기본값과 Compose 환경은 변경하지 않아 다른 개발자의 환경에 영향을 주지 않는다. | 로컬 데이터소스, Flyway·JPA 기동 | 백엔드 로컬 개발 환경 | 접속 확인: PostgreSQL 17.10. 비밀번호 값은 의사결정 기록에서 제외 |
| DEC-20260906-030 | 2026-09-06 | 에이전트 자동 결정 | 확정·유지 | 병합 결과에 맞춰 참조 코드를 제거 / 두 번째 부모 `e981611`에 존재하던 누락 파일만 복원 | 병합 커밋 `e41ad83`에서 빠진 공통 기반 소스, Flyway V2~V6, 관련 테스트를 두 번째 부모의 내용 그대로 복원한다. 현재 파일이나 병합 후 변경은 덮어쓰지 않는다. | 현재 소스가 `PostStatus`, `PlaceEntity`, `PagingProperties` 등 누락 타입을 계속 참조해 컴파일이 불가능하며, 해당 파일들은 병합 직전 두 번째 부모에서 정상 추적되었다. 참조 코드를 대량 제거하면 기존 게시글·장소 기능 범위를 축소하고 API·DB 계약을 훼손한다. 누락 파일만 복원하는 방식은 되돌리기 쉽고 병합 의도를 가장 작게 복구한다. | 백엔드 빌드, 게시글·장소 공통 기반, Flyway 초기 스키마, 관련 테스트 | 기존 백엔드 전체 빌드 및 DEC-20260903-015 | `e41ad83`의 첫 번째 부모 및 현재 작업 트리 파일은 유지 |
| DEC-20260906-031 | 2026-09-06 | 에이전트 자동 결정 | 확정·유지 | 신규 호출부를 이전 계약으로 일괄 후퇴 / 병합 직전 공통 인터페이스 계약 복구 | `CurrentUserProvider.optional`, `TierDecisionLogger`의 `TierInput` 인자, `TierResultResponse.tierOnly`를 병합 직전 두 번째 부모 계약대로 복구한다. | 병합 결과에서 공통 인터페이스만 이전 형태가 되었지만 다수의 최신 호출부와 JPA 구현은 확장된 계약을 사용한다. 호출부를 후퇴시키면 비회원 조회, 판정 입력 영속화, 로그 없는 게시글 응답 기능이 소실된다. 기존 계약 복구가 API·DB 동작을 보존하는 최소 수정이다. | 인증 선택 조회, 게시글 등급 판정 로그·응답, 백엔드 컴파일 | AUTH-011, PST-028, PST-033, PST-034, PST-047 | `e981611`의 공개 계약과 일치 |
| DEC-20260906-032 | 2026-09-06 | 사용자 | 확정·로컬 개발 | 빈 JWT 비밀키 유지 / 로컬 전용 난수 키 생성 | 암호학적 난수로 생성한 32바이트 이상의 JWT 비밀키를 Git에서 제외된 루트 `.env`의 `SNAPHERE_JWT_SECRET`에 저장한다. 실제 키 값은 문서·로그·응답에 기록하지 않는다. | 로컬 애플리케이션 기동 시 HS256 서명 키가 필요하며 현재 `.env`에는 해당 변수가 없다. 로컬 키를 저장소와 분리해 인증 기능을 실행한다. | 로컬 JWT 발급·검증, 개발 환경 비밀 관리 | AUTH-007~AUTH-009, 백엔드 로컬 실행 | 운영 키로 사용하지 않음 |
| DEC-20260906-033 | 2026-09-06 | 사용자 | 확정·로컬 테스트 | `snaphere_demo`의 기존 객체·데이터 보존 / 전체 초기화 후 독립 ERD SQL 재적용 | 로컬 테스트 DB `snaphere_demo`의 `public` 스키마를 초기화하고 `Downloads/01_snaphere_schema.sql`, `Downloads/02_snaphere_dummy_data.sql`을 순서대로 실행해 28테이블 독립 ERD 스키마와 더미 데이터를 구성한다. | 사용자가 테스트용 로컬 DB라 기존 데이터를 폐기해도 된다고 명시하고 두 SQL의 용도를 지정했다. | 로컬 `snaphere_demo` 데이터 보존, 독립 ERD 테스트 환경 | 데이터 설계 v1.1.3 참조본, 로컬 DB 검증 | DEC-20260906-029의 DB 접속 선택은 유지. 이 DB는 현재 UUID 기반 Spring Boot Flyway 실행용으로 사용하지 않음 |
| DEC-20260906-034 | 2026-09-06 | 사용자 | 확정·로컬 개발 | `snaphere_demo`를 독립 ERD SQL 테스트용으로 유지 / Spring Boot Flyway 전용 DB로 전환 | `snaphere_demo`의 독립 ERD 테이블과 더미데이터를 삭제하고, 빈 `public` 스키마에서 애플리케이션 Flyway V1~V18을 적용하는 로컬 실행 DB로 전환한다. 이후 독립 SQL은 이 DB에 실행하지 않는다. | 사용자가 Flyway 오류 해결을 위해 `snaphere_demo`를 Flyway 전용 DB로 바꾸도록 명시했다. | 로컬 데이터 보존, Flyway 이력, Spring Boot 기동 | 백엔드 로컬 개발 환경, Flyway V1~V18 | DEC-20260906-033을 대체. DEC-20260906-029의 접속 대상은 유지 |
| DEC-20260906-035 | 2026-09-06 | 사용자 | 확정·유지 | Flutter 화면의 더미·정적 데이터 유지 / 현재 구현된 백엔드 API 연결 및 로컬 통합 검증 | 현재 구현된 Flutter 페이지를 현재 제공되는 백엔드 API에 연결하고, 로컬 DB·서버를 대상으로 응답과 앱 테스트를 검증한다. 기존 Riverpod·Repository 경계를 유지하며 API나 DB 계약은 변경하지 않는다. | 사용자가 앱과 지금까지 구현된 API의 실제 연결 및 테스트를 명시적으로 요청했다. | Flutter 데이터 계층·화면 상태·로컬 통합 검증 | 현재 구현된 앱 페이지 및 API 전체 | 백엔드의 기존 작업트리 변경은 수정하지 않음 |
| DEC-20260906-036 | 2026-09-06 | 사용자 | 확정·로컬 개발 | 독립 ERD용 `02_snaphere_dummy_data.sql`을 그대로 실행 / 데이터를 현재 Flyway V1~V18 스키마에 맞게 변환하여 적재 | 원본 더미 데이터의 의미와 관계를 가능한 범위에서 보존하되, 현재 Spring Boot가 사용하는 UUID 기반 Flyway 스키마 및 제약조건에 맞춘 별도 로컬 시드 SQL로 변환하여 `snaphere_demo`에 적재한다. Flyway 스키마 자체와 원본 다운로드 SQL은 변경하지 않는다. | 사용자가 현재 앱에서 사용할 수 있도록 Flyway 스키마에 맞게 변환하여 넣도록 명시했다. | 로컬 `snaphere_demo` 데이터, 개발용 시드 SQL, 앱·API 통합 테스트 데이터 | Flyway V1~V18, 현재 구현된 앱 페이지 및 API | DEC-20260906-034의 Flyway 전용 DB 원칙은 유지하며, 독립 SQL 직접 실행 금지 대신 호환 시드를 사용 |
| DEC-20260906-037 | 2026-09-06 | 사용자 | 확정·로컬 개발 | Spring Boot를 Gradle `bootRun`으로만 실행 / VS Code에서 `SnapHereApplication.main()` 직접 실행도 지원 | VS Code Java 컴파일러가 Gradle의 `-parameters` 설정과 동일하게 메서드 매개변수 메타데이터를 생성하도록 프로젝트 설정을 추가한다. 중복된 Java 실행 구성은 루트 `.env`, `snaphere-api` 프로젝트명 및 백엔드 작업 디렉터리를 사용하는 하나의 `SnapHereApplication` 구성으로 정리한다. | 사용자가 IDE의 `main()` 직접 실행에서도 API가 정상 동작하도록 VS Code 설정과 중복 실행 구성을 정리하도록 명시했다. | VS Code Java 빌드·디버그, Spring MVC 요청 파라미터 바인딩, 로컬 환경변수 로딩 | 백엔드 로컬 실행 환경, API 통합 테스트 | Gradle `bootRun` 지원은 그대로 유지 |
| DEC-20260906-031 | 2026-09-06 | 사용자 | 확인 필요·진행 중 | PostGIS 3.5.7 유지 / PostGIS 3.6.2로 변경 | 현재 개발·테스트·운영 기준의 PostGIS를 3.6.2로 변경한다. Percona PostgreSQL 17.10.2와 JDK 21.0.11, Gradle 8.14 고정은 유지한다. | 사용자가 현재 3.5.7 설정을 3.6.2로 바꾸라고 명시했다. 다만 Percona 공식 17.10.2 및 최신 17.11.1 배포판은 PostGIS 3.5.7만 제공하므로, 커스텀 이미지 빌드 또는 Percona 제외 중 구현 방식을 사용자에게 확인해야 한다. | DB 컨테이너 이미지, Testcontainers, 통합 테스트, 독립 SQL·DB·실행 문서 | 백엔드 DB·공간 확장 버전 고정 | DEC-20260905-001·010의 PostGIS 버전을 대체할 예정. 구현 방식 확정 전에는 이미지·테스트 기대값을 변경하지 않는다. |
| DEC-20260906-032 | 2026-09-06 | 사용자 | 확정·유지 | Percona를 제외한 공식 PostGIS 이미지로 교체 / Percona PostgreSQL 17.10.2 기반 커스텀 이미지에서 PostGIS 3.6.2 소스 빌드 | Percona PostgreSQL 17.10.2 기반 이미지를 유지하고 PostGIS 3.6.2 공식 소스를 SHA-256으로 검증해 다단계 빌드한다. 결과 이미지는 `snaphere/percona-postgresql-with-postgis:17.10.2-postgis3.6.2`로 고정하며 Compose와 Testcontainers가 같은 Dockerfile을 사용한다. 빌드 도구와 헤더는 최종 런타임 이미지에 포함하지 않는다. | 사용자가 Percona 17.10.2를 유지하는 커스텀 이미지 방식을 명시적으로 승인했다. 공식 Percona 17.10.2·17.11.1 배포판은 PostGIS 3.5.7까지만 제공하므로 3.6.2를 정확히 맞추려면 별도 빌드가 필요하다. | DB 컨테이너 빌드, 로컬 Compose, 통합 테스트, 실행·DB·버전 문서 | 백엔드 DB·공간 확장 버전 고정 | DEC-20260906-031의 구현 방식을 확정하고 DEC-20260905-001·010의 PostGIS 3.5.7 설정을 대체한다. PostgreSQL·JDK·Gradle 결정은 유지한다. |
| DEC-20260906-033 | 2026-09-06 | 에이전트 자동 결정 | 확정·유지 | 래스터·Protobuf까지 포함한 전체 PostGIS 빌드 / 현재 요구사항에 필요한 PostGIS 핵심 확장만 빌드 | 커스텀 PostGIS 3.6.2는 `--without-raster --without-protobuf`로 빌드한다. `postgis` 핵심 확장과 geography, GiST, `ST_DWithin` 등 현재 서비스가 사용하는 공간 기능은 포함한다. | 현재 ERD와 MAP 구현은 래스터 또는 MVT 생성 함수를 사용하지 않는다. 사용하지 않는 네이티브 의존성과 이미지 크기를 줄이는 되돌리기 쉬운 빌드 세부사항이다. 향후 래스터 분석이나 서버 측 MVT가 요구되면 해당 옵션을 제거하고 필요한 런타임 라이브러리를 추가한다. | DB 컨테이너 빌드 및 제공 PostGIS 기능 범위 | MAP-001~MAP-030, PLC-005·007, SYS-006 | DEC-20260906-032의 커스텀 이미지 빌드 세부사항. API·DB 스키마 계약은 바뀌지 않는다. |
| DEC-20260906-034 | 2026-09-06 | 에이전트 자동 결정 | 확정·유지 | Docker 29에서 DB 통합 테스트를 건너뜀 / Testcontainers Docker API를 1.40으로 명시 | Gradle `Test` 작업에 docker-java의 `api.version=1.40` 기본 시스템 속성을 설정한다. 실행자가 같은 시스템 속성을 직접 지정하면 그 값을 우선한다. | 현재 Docker Engine 29는 최소 API 1.40을 요구하지만 Testcontainers 1.21.3의 docker-java 기본값으로는 `/info` 요청이 400이 되어 `disabledWithoutDocker` 테스트가 모두 건너뛰었다. 1.40은 현재 엔진이 지원하는 가장 낮은 값이라 더 높은 API로 고정하는 것보다 호환 범위가 넓으며, 테스트 실행에만 영향을 주는 되돌리기 쉬운 설정이다. | Gradle 테스트 실행, Testcontainers 기반 Flyway·DB 통합 검증 | DB·MAP·RNK 통합 테스트 | DEC-20260906-032의 동일 이미지 통합 검증을 실제 실행하기 위한 테스트 환경 설정. 운영 런타임에는 영향이 없다. |
| DEC-20260906-035 | 2026-09-06 | 사용자 | 확정·유지 | SCH 전체를 백엔드와 Flutter에 구현 / 백엔드만 구현, RNK 브랜치에 누적 / 별도 SCH 브랜치 | SCH-001~SCH-011은 백엔드 API·검색 인덱스·로그·Redis·문서·테스트까지만 구현한다. Flutter 더미 검색 화면은 변경하지 않고, 현재 RNK·버전 고정 커밋에서 `feature/backend-search-sch001-011` 브랜치를 분기한다. | 사용자가 구현 범위를 백엔드로 한정하고 기능 브랜치 분리를 선택했다. | 저장소 작업 범위, Git 브랜치, 검증 범위 | SCH-001~SCH-011 | 원격 push와 PR 생성은 별도 요청 전까지 하지 않는다. |
| DEC-20260906-036 | 2026-09-06 | 사용자 | 확정·유지 | 최근 검색어 DB·Redis / 인기 집계 24시간·7일·전체 / 보존 30일·90일·무기한 | 사용자별 최근 검색어는 Redis에 최대 20개, 마지막 검색부터 30일간 보관한다. 인기 검색어는 최근 7일의 `search_logs`를 집계하고 원본 로그는 30일 보관한다. 커서 없는 첫 검색만 인기·최근 기록으로 센다. | 검색 초기 화면에 충분한 개수와 최근 흐름을 제공하면서 더보기 호출로 순위가 부풀거나 로그가 무한 누적되는 것을 막는다. | 최근 검색 저장, 인기 검색 집계, 데이터 보존·삭제 배치 | SCH-002, SCH-010, SCH-011 | Redis 장애 시 최근 검색어 유실 가능성을 사용자가 수용했다. |
| DEC-20260906-037 | 2026-09-06 | 사용자 | 확정·유지 | 더보기 커서가 타입을 암시 / 커서 요청에 단일 타입 요구, 지역명은 정보만 반환·문자 검색 병행 / 지역 필터로 전환, 인기·최신 우선 / 일치도 우선 | 통합검색 첫 요청은 여러 타입을 조회하지만 커서 요청은 `types` 한 개만 허용한다. 검색어가 표준 지역명과 정확히 일치하면 그 지역을 기존 `areaCode`보다 우선하는 필터로 전환한다. 결과는 정확 일치·접두어·부분 일치 순으로 두고 동률에서 인기·최신 지표를 사용한다. | 단일 `cursor` 계약으로 타입별 더보기를 모호하지 않게 만들고 SCH-008의 지역 필터 전환을 한 번의 응답으로 완성한다. | 통합검색 API 동작, 커서 계약, 지역 필터, 검색 정렬 | SCH-001, SCH-003~SCH-009, SYS-003~SYS-004 | API 필드 구조는 변경하지 않는다. |
| DEC-20260906-038 | 2026-09-06 | 에이전트 자동 결정 | 확정·유지 | offset 커서 / 정렬 키 keyset 커서, Redis 장애 시 503 / 보조 기능 fail-open, 로그 삭제 시각 미정 / 05:20 KST | 타입별 정렬 키와 검색 조건 지문을 담은 keyset 커서를 사용한다. 인기 검색 캐시는 Redis에 10분 보관하되 장애 시 DB로 폴백하고, 최근 검색 기록 실패는 본 검색 응답을 막지 않는다. 검색 로그 정리는 기존 05:00 계정 파기·05:10 팔로우 보정과 겹치지 않게 매일 05:20 KST에 실행하며 cron은 설정으로 바꿀 수 있게 한다. | 공개 검색 결과가 변하는 동안 offset 중복·누락을 줄이고 보조 저장소 장애가 핵심 검색을 중단하지 않게 한다. 05:20은 기존 배치와 실행 시각만 분리하는 저위험 운영 세부사항이다. | 검색 페이징, Redis 장애 복구, 배치 운영 설정 | SCH-001~SCH-003, SCH-010~SCH-011, SYS-003~SYS-004 | 기존 PostgreSQL·Redis 의존성만 재사용하며 신규 운영 의존성은 없다. |
| DEC-20260907-001 | 2026-09-07 | 사용자 | 확정·유지 | SYS-011~021을 백엔드와 Flutter에 모두 구현 / 백엔드만 구현, Swagger 추가 / 기존 XLSX·Markdown 명세 유지 | SYS-011~021은 백엔드까지만 구현하고 Flutter 화면 요구사항인 SYS-011은 후속 클라이언트 작업으로 남긴다. Swagger·springdoc 의존성은 추가하지 않고 기존 API XLSX와 Markdown을 정본으로 유지·보강한다. | 사용자가 백엔드 구현 범위와 기존 문서 체계 유지를 선택했다. | 구현 범위, API 문서, 프로덕션 의존성 | SYS-011~SYS-013 | Flutter와 Swagger는 이번 구현 범위에서 제외한다. |
| DEC-20260907-002 | 2026-09-07 | 사용자 | 확정·유지 | 관리자 배치 일부만 지원 / 5종 모두 지원, 신고 검토 대상 4종 / POST·PLACE만 지원, 검토 메모 저장 / 메모 제거 | 관리자 수동 배치는 `PLACE_SYNC`, `EVENT_SYNC`, `RANKING_RECALC`, `HEATMAP_RECALC`, `COUNTER_RECONCILE` 5종을 지원한다. 신고 검토 API는 POST와 PLACE만 허용하며 ERD에 없는 `memo`는 요청 계약에서 제거한다. | 사용자가 SYS 운영 범위와 최신 ERD 기준의 API 계약을 명시했다. | 관리자 API, 배치 실행, 신고 검토 계약 | SYS-015, SYS-017 | 신고 상태는 PENDING·RESOLVED·REJECTED로 통일한다. |
| DEC-20260907-003 | 2026-09-07 | 사용자 | 확정·유지 | 모든 응답을 동일 TTL로 캐시 / 정적·비개인화 데이터만 구분 캐시 | 시도·시군구 목록은 24시간, 개인화되지 않은 언어별 장소 상세 본문은 10분 Redis 캐시를 적용한다. 지도·검색 기존 캐시는 유지하고 Redis 장애 시 PostgreSQL에서 직접 조회한다. | 데이터 변경 주기와 개인정보·실시간 카운터의 신선도를 함께 보장하기 위한 사용자 선택이다. | Redis 키·TTL, 장소 조회, 장애 복구 | SYS-019, PLC-001~PLC-013 | 조회수·북마크·랭킹·최근 게시글 등 변동·개인화 필드는 상세 본문 캐시에서 제외한다. |
| DEC-20260907-004 | 2026-09-07 | 사용자 | 확정·유지 | 업로드 원본을 바로 공개 / 비공개 원본 검역 후 정제본만 공개 | 게시글 사진 원본은 `originals/` 비공개 경로에 저장하고 EXIF 제거·재인코딩을 완료한 공개 이미지와 썸네일만 응답한다. 모든 사진이 준비되기 전 게시글은 공개 목록·상세·검색·지도·랭킹에서 숨기며 생성 응답은 게시글 ID와 미디어 처리 상태 중심의 최소 응답으로 바꾼다. | 원본 위치정보와 비정제 콘텐츠가 공개되는 시간 구간을 제거하라는 사용자 선택이다. | 미디어 키 규칙, 게시글 공개 조건, API 응답 계약 | SYS-021, JOB-003, PST-001~PST-021 | ERD는 변경하지 않고 기존 이미지 키·해시·썸네일 값으로 공개 준비 여부를 판정한다. |
| DEC-20260907-005 | 2026-09-07 | 사용자 | 확정·유지 | 미디어 상태 컬럼·테이블 추가 / ERD 유지, 무제한 재시도 / 최대 5회 | 미디어 처리 상태와 시도 횟수는 Redis에 보관하고 DB 스키마는 유지한다. 실패 작업은 5분 간격으로 최대 5회 재시도하며 최종 실패 게시글은 계속 비공개로 둔다. | 사용자가 최신 ERD 구조를 유지하면서 운영 재시도 기능을 구현하도록 결정했다. | Redis 미디어 상태, 재시도 스케줄, 게시글 노출 | SYS-021, JOB-003 | Redis 유실 시 시도 횟수는 초기화될 수 있으나 원본은 계속 비공개다. |
| DEC-20260907-006 | 2026-09-07 | 사용자 | 확정·유지 | 행사 동기화 기간·시각 미정 / 최근 30일부터 향후 1년, 매일 04:30 KST | 행사 동기화는 실행일 기준 30일 전부터 1년 후까지 조회하며 매일 04:30 Asia/Seoul에 실행한다. 관리자 수동 실행도 같은 기간 규칙을 사용한다. | 운영할 행사 데이터의 범위와 갱신 시각을 사용자와 확정했다. | TourAPI 호출, 행사 UPSERT, 스케줄러 | SYS-015, EVT-001~EVT-004, JOB-002 | cron은 환경 설정으로 변경할 수 있게 한다. |
| DEC-20260907-007 | 2026-09-07 | 에이전트 자동 결정 | 확정·유지 | Redis 장애 시 미디어 원본 공개 / 게시글 비공개 유지, 캐시 장애 시 5xx / DB 폴백 | Redis 장애 시 일반 조회 캐시는 DB로 폴백한다. 미디어 처리 상태 저장·잠금이 실패하면 비정제 원본을 노출하지 않고 게시글을 비공개 상태로 유지한 채 후속 재시도를 기다린다. | 캐시는 보조 계층이라 가용성을 우선할 수 있지만 미디어 원본 공개는 개인정보 유출로 이어질 수 있어 보안을 우선해야 한다. 원본 공개 대안은 SYS-021 목적과 충돌하므로 제외했다. | 장애 처리, 개인정보 보호, 게시글 가시성 | SYS-019, SYS-021, JOB-003 | API·DB 계약을 늘리지 않는 보수적 장애 처리다. |
---

## PR #47 충돌 해결 검토안

| 결정 ID | 날짜 | 주체 | 상태 | 선택지·배경 | 결정 내용 | 근거 | 영향 범위 | 관련 요구사항 | 대체/비고 |
|---|---|---|---|---|---|---|---|---|---|
| DEC-20260907-008 | 2026-09-07 | 에이전트 자동 결정 | 제안·원격 미적용 | PR 이력 재작성 / 최신 develop을 기존 PR 브랜치에 병합 | 독립 임시 복제본에서 PR #47과 develop `7a663f6`의 병합 결과를 검토한다. `PlaceRepository.posts`의 동일 SQL 표현식에 생긴 줄바꿈 충돌은 develop 형태로 정리한 로컬 수정안을 검증한다. PR 브랜치 반영은 사용자 확인 전까지 수행하지 않는다. | 사용자가 PR 머지 불가 해결을 요청했다. 실제 충돌은 동일한 `coalesce(pi.aspect_ratio,1)`와 게시글 열 목록의 줄 배치 차이이다. 자동 승인 검토가 브랜치 통합·push 결정에 사용자 확인을 요구해, 원격 변경 없이 검토 가능한 수정안만 준비한다. | PR #47 로컬 충돌 수정안·검증, 원격 적용 보류 | PLC-013, API-PLC-003, 협업 규칙 | DEC-20260903-015의 기존 기능 보존 원칙 참조. API·DB 계약 및 기존 의사결정 변경 없음 |

검증 결과 (2026-09-07, 임시 복제본): JDK 21.0.11·Gradle 8.14의 `gradlew.bat test --offline --console=plain` 성공. 총 448개 중 441개 통과, Docker 환경 부재로 DB 통합 테스트 7개 건너뜀. `dart format .`은 62개 파일 변경 없음, `flutter analyze --no-pub` 문제 없음, `flutter test --no-pub` 단위·위젯 통합 25개 통과. 해결된 `PlaceRepository.java`는 develop `7a663f6`과 동일하다. 원격 브랜치에는 아직 적용하지 않았다.

## PR #47 충돌 해결 반영 승인

| 결정 ID | 날짜 | 주체 | 상태 | 선택지·배경 | 결정 내용 | 근거 | 영향 범위 | 관련 요구사항 | 대체/비고 |
|---|---|---|---|---|---|---|---|---|---|
| DEC-20260907-009 | 2026-09-07 | 사용자 | 확정·실행 승인 | 로컬 검토안 보류 / 검증된 병합 결과를 기존 PR 브랜치에 반영 | develop `7a663f6`을 통합하고 장소 게시글 SQL 충돌을 해결한 검증본을 merge 커밋으로 확정한 뒤 `feature/common-api-integration`에 일반 push한다. 원격 develop과 PR 브랜치가 검증 시점과 동일함을 확인하고 반영한다. | 사용자가 충돌 해결 결과와 테스트 결과를 확인한 뒤 "push해줘"라고 명시적으로 승인했다. | PR #47 브랜치 갱신, 장소 게시글 조회 병합, 의사결정 이력 | PLC-013, API-PLC-003, 협업 규칙 | DEC-20260907-008의 원격 적용 보류를 대체. GitHub PR의 최종 merge는 포함하지 않음 |

## PR #47 최종 병합과 로컬 동기화 요청

| 결정 ID | 날짜 | 주체 | 상태 | 선택지·배경 | 결정 내용 | 근거 | 영향 범위 | 관련 요구사항 | 대체/비고 |
|---|---|---|---|---|---|---|---|---|---|
| DEC-20260907-010 | 2026-09-07 | 사용자 | 확정·실행 요청 | PR 브랜치 push 후 종료 / 머지 가능 확인 후 PR 병합과 git pull까지 수행 | GitHub에서 PR #47의 머지 가능 상태가 가능으로 바뀌면 PR을 머지하고 git pull한다. | 사용자 요청: "github에서 머지 가능 상태가 가능으로 바뀌면 머지하고 git pull해주세요" | GitHub PR #47 병합, 로컬 저장소 동기화 | PR #47, 협업 규칙 | DEC-20260907-009의 최종 merge 제외 범위를 이번 요청으로 대체. 사용자는 구체적인 병합 방식을 지정하지 않았다. |

## PR #47 GitHub 인증 사용 승인

| 결정 ID | 날짜 | 주체 | 상태 | 선택지·배경 | 결정 내용 | 근거 | 영향 범위 | 관련 요구사항 | 대체/비고 |
|---|---|---|---|---|---|---|---|---|---|
| DEC-20260907-011 | 2026-09-07 | 사용자 | 확정·실행 승인 | 저장된 인증의 API 사용 보류 / PR 조회·머지에 사용 | 저장된 GitHub 인증을 PR #47 조회·머지 API에 사용한다. 인증 정보는 화면·로그에 노출하지 않는다. | 해당 인증 사용 승인 질문에 사용자가 "사용해도돼"라고 답했다. | GitHub PR #47 인증 조회·머지 | PR #47, 협업 규칙 | DEC-20260907-010 실행을 위한 기존 인증 사용 승인. 인증 값은 이 기록에 포함하지 않음 |

실행 결과 (2026-09-07): GitHub REST의 `mergeable=true`, `mergeable_state=clean`과 GraphQL의 `MERGEABLE/CLEAN`을 확인한 뒤 검증된 PR head `cb141e4`를 머지했다. GitHub가 반환한 merge 커밋은 `531c5737966fb61323aee7aec06d3d46136011db`다. 원래 저장소의 develop에서 `git pull --ff-only --no-rebase origin develop`을 실행해 같은 커밋으로 갱신했다. 기존 `backend/bin` 401개 파일은 백업 확인 후 복원했으며, 복구용 stash `05ca3720f7e5ab37b2fa23dfae65eb9d3b1ae0ae`를 남겼다. 위 두 결정은 실행 전 임시 복제본에 기록했고, pull 후 원래 저장소에 옮겼다. 이번 기록은 로컬 변경으로 보존한다.

## 홈 지도·마이페이지 Figma 구현

| 결정 ID | 날짜 | 주체 | 상태 | 선택지·배경 | 결정 내용 | 근거 | 영향 범위 | 관련 요구사항 | 대체/비고 |
|---|---|---|---|---|---|---|---|---|---|
| DEC-20260907-012 | 2026-09-07 | 사용자 | 확정·실행 요청 | 기존 화면 유지 / 지정한 Figma 두 파트 구현 | develop에서 새 feature 브랜치를 만들어 Figma `92:311` 홈 지도와 `92:1931` 마이페이지의 하위 화면을 구현하고 기존 API를 연결한다. `docs/09-git-strategy.md`와 커밋 컨벤션에 따라 기능 단위 커밋을 생성한다. | 사용자가 두 Figma 링크·신규 브랜치·API 연결·커밋 생성을 명시했다. | Flutter 홈 지도, 내·다른 사용자 프로필, 팔로우 목록, 게시글 빈 상태, 뱃지·방문 지도 | MAP-001~025, USER-001~002·008~010, SOC-001~003, VST-007~009, BDG-009~010 | 이번 요청에는 push·PR 생성이 포함되지 않는다. 백엔드 API·DB 계약은 유지하며 실제 지도 패키지 추가 여부는 별도 확인한다. |

| DEC-20260907-013 | 2026-09-07 | 에이전트 자동 결정 | 제안·사용자 확인 필요 | Google Maps SDK 연결 / Figma 정적 지도 채택 | 기존 DEC-20260905-003의 Google Maps 선택에 맞춰 공식 Flutter 패키지 `google_maps_flutter` 2.18.0과 Android·iOS 지도 키 연결을 제안한다. 신규 프로덕션 의존성 추가에 대한 명시적 확인 전에는 패키지·네이티브 지도 설정을 변경하지 않는다. | 자동 승인 검토가 기존 지도 제공자 결정만으로 새 프로덕션 패키지 추가까지 승인된 것으로 볼 수 없다고 판단했다. 지도와 독립적인 프로필·팔로우·뱃지 목록 API 구현 및 검증은 진행한다. | 지도 패키지·플랫폼 초기화 승인, 홈 지도·방문 지도 구현 보류 | MAP-001~025, VST-007~009, DEC-20260905-003 | 아직 패키지를 추가하지 않았다. 키 값은 문서·로그·커밋에 포함하지 않는다. 전체 요청 완료 후 기능 단위 커밋을 생성할 예정 |

| DEC-20260907-014 | 2026-09-07 | 사용자 | 확정·실행 승인 | 지도 SDK 추가 보류 / 공식 Flutter SDK·플랫폼 키 연결 승인 | `google_maps_flutter` 2.18.0을 추가하고 Android·iOS 지도 키 연결을 진행한다. 기존 위치 권한과 Riverpod·Repository 구조를 유지하며, 키 값은 커밋하지 않는다. | 패키지 버전 및 Android·iOS 지도 키 연결을 명시한 승인 요청에 사용자가 "승인"이라고 답했다. | Flutter 지도 의존성, Android·iOS 초기화, 홈 지도·방문 지도 구현 및 기능별 커밋 | MAP-001~025, VST-007~009, DEC-20260907-012 | DEC-20260907-013의 승인 대기를 대체한다. Google Cloud 과금·키 제한 정책 변경은 포함하지 않는다. |

## 실기기 UX·연결 검증

| 결정 ID | 날짜 | 주체 | 상태 | 선택지·배경 | 결정 내용 | 근거 | 영향 범위 | 관련 요구사항 | 대체/비고 |
|---|---|---|---|---|---|---|---|---|---|
| DEC-20260907-015 | 2026-09-07 | 사용자 | 확정·실행 요청 | 에뮬레이터 검증으로 종료 / 연결한 실기기에서 UX·연결 오류를 재현하고 수정 | 연결된 Android 실기기에서 기존 앱의 주요 동선을 직접 검증하고, 어색한 UX와 끊긴 연결을 기존 구조·API 계약 안에서 수정한다. 앱 데이터 초기화·계정 삭제·실사용 콘텐츠 게시 같은 파괴적 검증은 하지 않는다. | 사용자가 실기기를 연결한 뒤 직접 테스트하면서 UX 및 미연결 부분을 찾아 수정해 달라고 요청했다. | Flutter 홈·프로필과 연결 동선, 인증 안내·화면 복귀, 네트워크 오류 처리, 실기기 검증·회귀 테스트 | MAP-001, MAP-007, USER-001, USER-005, AUTH-010, AUTH-012, SYS-002 | DEC-20260907-012·014 후속 검증. 신규 운영 의존성·보안·API·DB 계약 변경과 별도 미구현 기능의 확대는 먼저 확인한다. |

| DEC-20260907-016 | 2026-09-07 | 에이전트 자동 결정 | 확정·기존 UX 오류 수정 | 로그인 안내를 별도 샘플 화면으로 유지 / 실제 탐색 화면 위 안내와 원래 동선 보존 | 로그인 안내는 현재 화면 위에 표시하고 취소·뒤로가기는 원래 화면으로 복귀한다. 비회원도 로그인 페이지에는 진입할 수 있도록 잘못된 라우팅을 수정하되 보호된 페이지·변경 요청의 기존 인증 검사는 유지한다. 지역 게시글 시트에서 Android 뒤로가기는 앱 종료 전에 시트를 닫는다. | 실기기에서 로그인 버튼이 홈으로 되돌아가는 오류, 전북 샘플 배경 노출, 시트에서 뒤로가기가 앱을 종료하는 문제를 재현했다. 홈으로 일괄 이동하는 대안은 탐색 상태를 잃게 하므로 기존 go_router 스택을 보존하는 저위험 UI 교정을 선택했다. | 로그인 안내·취소·재진입, 홈 시트·복귀, 위젯 회귀 테스트 | AUTH-010, AUTH-012, MAP-007 | DEC-20260907-015의 구체화. 토큰·계정·인증 제공자·API·DB 계약은 변경하지 않는다. |

| DEC-20260907-017 | 2026-09-07 | 에이전트 자동 결정 | 확정·기존 설정 연결 복구 | 매 실행마다 별도 dart-define 수동 전달 / 이미 로드한 앱 OAuth 설정을 기존 Google SDK 초기화에 연결 | 기존 `GOOGLE_SERVER_CLIENT_ID` dart-define 우선순위를 유지하고, 미지정 시 앱 `.env`의 기존 `GOOGLE_OAUTH_CLIENT_ID`를 Google SDK의 `serverClientId`로 전달한다. 같은 이름의 dart-define과 `GOOGLE_SERVER_CLIENT_ID` 환경 파일 값도 지원한다. 값은 로그·문서·커밋에 기록하지 않는다. | 실기기에서 `serverClientId must be provided on Android` 오류를 확인했다. 앱과 백엔드의 기존 OAuth 클라이언트 ID가 일치하지만 앱 구현은 `.env`를 읽지 않고 별도 dart-define만 사용했다. 새 인증 제공자·클라이언트 생성 없이 이미 정한 동일 OAuth 설정을 연결하는 저위험 수정이다. | Google 로그인 초기화, 설정 우선순위 테스트, 실행 문서 | AUTH-001, AUTH-010, DEC-20260907-015 | 토큰 검증·저장·인증 제공자·클라우드 설정은 변경하지 않는다. OAuth client secret은 앱에 넣지 않는다. |

| DEC-20260907-018 | 2026-09-07 | 에이전트 자동 결정 | 확정·기존 로그인 오류 복구 | 모든 실패를 일반 로그인 오류로 표시 / 기존 API 오류·네트워크 실패를 구분하여 복구 방법 안내 | Google 로그인 교환의 서버 오류 코드와 연결 실패·시간 초과를 기존 AuthFailure로 변환해 안내한다. 비회원 세션은 보존하고 토큰·계정 정보·서버 원문을 화면이나 로그에 노출하지 않는다. 실기기 재연결로 사라진 ADB reverse 8080을 복구하고, 변경된 로컬 OAuth 설정은 에이전트가 시작했던 테스트 백엔드에 재시작으로 반영한다. | Google 인증 결과 수신 뒤 일반 로그인 오류가 발생했다. 실기기 포트 연결이 비어 있었으며, 백엔드는 12:18부터 실행된 상태인데 로컬 OAuth 설정 파일은 13:57에 변경됐다. API·네트워크 예외가 현재 일반 예외 처리에 흡수되어 원인 구분도 불가능했다. 기존 연결·설정과 오류 처리를 복구하는 저위험 수정이며 인증 검증 우회는 대안에서 제외한다. | 실기기 로컬 API 연결, Google 토큰 교환 오류 안내, 테스트 백엔드 설정 재로딩·회귀 테스트 | AUTH-001, AUTH-010, SYS-002, DEC-20260907-015·017 | API·DB·토큰 검증·서명 키·클라우드 정책은 변경하지 않는다. 사용자 IDE 실행 설정은 보존한다. |

| DEC-20260907-019 | 2026-09-07 | 에이전트 자동 결정 | 확정·기존 입력란 레이아웃 복구 | 전역 검색창 테마의 높이 변경 / 프로필 설정 입력란에서만 고정 높이 해제 | 프로필 설정의 입력 장식에서 전역 검색창용 38 높이 제약을 해제하여 소개글의 기존 4줄·160자 입력과 글자 수 표시가 필요한 높이를 확보한다. 다른 화면의 검색창 테마는 유지하고 사용자 입력은 hot reload로 보존한다. | 로그인 성공 후 실기기에서 소개글 테두리가 납작해지고 입력 내용이 보이지 않는 현상을 확인했다. 전역 테마 변경은 다른 검색창에 영향을 주므로 해당 화면에 한정한 저위험 레이아웃 교정을 선택한다. | 프로필 설정 닉네임·소개글 배치, 키보드·큰 글자 회귀 테스트 | USER-001, DEC-20260907-015 | API·DB·160자 제한·약관 동의 정책은 변경하지 않는다. 실계정 입력·동의·제출을 대신하지 않는다. |
| DEC-20260907-020 | 2026-09-07 | 사용자 | 확정·실행 요청 | 로컬 수정 유지 / 기능 단위 커밋 후 원격 브랜치 push 및 PR 생성 | 실기기 UX·로그인·프로필 입력란 수정 결과를 `docs/09-git-strategy.md`에 따라 기능 단위로 커밋하고 현재 feature 브랜치를 원격에 push한 뒤 `develop` 대상 GitHub PR을 생성한다. | 사용자 요청: "커밋생성하고 git-strategy에 따라 푸쉬하고 pr 생성해주세요" | 현재 feature 브랜치 커밋 이력, GitHub 원격 브랜치와 PR, 변경 문서 | DEC-20260907-015~019, 협업 규칙 | 사용자 소유 `backend/.vscode/launch.json`과 루트 `README.md`, 로컬 환경·비밀 값은 제외한다. PR merge는 포함하지 않는다. |

실행 결과 (2026-09-07): 실기기 UX 후속 변경을 기능별 5개 커밋으로 생성하고 기존 홈·마이 구현 커밋과 함께 `origin/feature/frontend-home-profile-map001-user001`에 push했다. 최신 `origin/develop` 대비 뒤처진 커밋이 없음을 확인했으며, `develop` 대상 GitHub PR #51을 생성했다. PR 생성 시점의 head는 `a2c7b06e15c743ba2da276ea875782fcd6a02f97`이다. 사용자 소유 `backend/.vscode/launch.json`과 루트 `README.md`는 작업 트리에 남겨 두고 커밋하지 않았다.

## 프론트엔드 공유 상세 · 상태 화면과 API 연결

| 결정 ID | 날짜 | 주체 | 상태 | 선택지·배경 | 결정 내용 | 근거 | 영향 범위 | 관련 요구사항 | 대체/비고 |
|---|---|---|---|---|---|---|---|---|---|
| DEC-20260909-001 | 2026-09-09 | 사용자 | 확정·실행 요청 | 일부 화면만 구현 / Figma 07·08·12 전체 구현 | `develop`에서 `feature/frontend-shared-detail` 브랜치를 만들고 Figma `Wireframe_v3`의 `07 Shared Detail`(알림·설정·게시글 상세·장소 상세), `08 Error & Empty States`, `12 Comment CRUD Prototype`을 구현한다. `chore/frontend-clean-code`의 계층 분리(domain → data → application → presentation)를 따르되 해당 브랜치를 베이스로 삼지는 않는다. | 사용자가 화면 번호 7·8·12를 지정했고, Figma `Wireframe_v1`과 `Wireframe_v3`에서 같은 번호가 다른 화면을 가리켜 v3 기준으로 확정했다. clean-code PR이 아직 develop에 병합되지 않아 베이스로 삼으면 계속 벌어진다. | Flutter 게시글·장소·알림·설정 화면, 공용 상태 위젯, 라우터 | PST-001~049, CMU-012~023, PLC-012~015, NTF-011~013, USER-002, SYS-010 | 백엔드·DB 계약은 변경하지 않는다. 알림은 컨트롤러가 없어 목 데이터로 동작한다. |
| DEC-20260909-002 | 2026-09-09 | 사용자 | 확정·실행 요청 | 화면만 마감 / 명세 97개 엔드포인트 전수 대조 후 프론트 연결 | 명세 `03-api-spec.md`의 97개 엔드포인트를 백엔드 컨트롤러·프론트 호출과 교차 대조하고, 백엔드가 구현된 것 중 프론트가 부르지 않던 엔드포인트를 모두 연결한다. | 화면 단위로만 보면 백엔드가 이미 제공하는 기능이 앱에서 빠진 것을 놓친다. 대조 결과 앱 대상 84개 중 30개가 미연결이었다. | 통합 검색·랭킹·지도·지역·태그·내 활동·설정·업로드·이벤트·소셜 | SCH-001~011, RNK-001~013, MAP-002~004, PLC-001~003, CMU-011~013, USER-003·006~008·011, VST-001~004, AUTH-005, SOC-005, EVT-002, PST-002·014 | 인기·팔로잉 피드(API-CMU-001·002)와 댓글 좋아요(API-CMU-009·010), 알림(API-NTF-001~004)은 백엔드가 없어 연결하지 못했다. |
| DEC-20260909-003 | 2026-09-09 | 에이전트 자동 결정 | 확정·검증 한계 명시 | 테스트 통과까지 확인 / 정적 분석까지만 보증 | `dart analyze`로 `lib`·`test` 전체를 검증하고 테스트 33건을 작성했으나 실행하지는 못했다. 결과를 "정적 분석 통과, 테스트 미실행"으로 보고한다. | 작업 PC의 `flutter_tester.exe`가 dill 로드 직후 `0xC0000409`(STATUS_STACK_BUFFER_OVERRUN)로 종료된다. 신규 `flutter create` 프로젝트의 기본 테스트도 동일하게 실패해 코드 문제가 아님을 확인했다. `build/`·`.dart_tool` 캐시 삭제로도 재현된다. | 검증 범위 보고, 테스트 실행 환경 | 해당 없음 | 엔진 아티팩트 재다운로드(`flutter precache --force`)는 사용자 승인 없이 수행하지 않았다. |

실행 결과 (2026-09-09): 공용 상태 위젯(`core/ui/state_views.dart`)과 상대 시각 표기(`core/ui/relative_time.dart`)를 분리하고, `features/post`·`place`·`notification`·`settings`·`region`·`activity`·`rankings`·`map` 계층을 추가했다. 플레이스홀더 라우트 5개 중 4개(`/photos/:photoId`, `/places/:placeId`, `/notifications`, `/regions/:regionId`, `/search`)를 실제 화면으로 교체하고 `/settings`·`/tags/:tagId`·`/me/activity`·`/photos/:photoId/comments`를 추가했다. 프론트 API 연결은 54/84에서 78/84로 늘었다. `dart format`·`dart analyze`는 통과했고 테스트 실행은 위 환경 문제로 하지 못했다.

원본 스프레드시트: [`specs/snaphere-requirements-spec-v1.1.7.xlsx`](specs/snaphere-requirements-spec-v1.1.7.xlsx) · [`specs/snaphere-api-spec-v1.1.7.xlsx`](specs/snaphere-api-spec-v1.1.7.xlsx)
변경 이력: [`08-spec-changelog.md`](08-spec-changelog.md)

## 프론트엔드 클린 코드 리팩토링

| 결정 ID | 날짜 | 주체 | 상태 | 선택지·배경 | 결정 내용 | 근거 | 영향 범위 | 관련 요구사항 | 대체/비고 |
|---|---|---|---|---|---|---|---|---|---|
| DEC-20260908-001 | 2026-09-08 | 사용자 | 확정·실행 요청 | 일부 화면만 수정 / Flutter 프론트엔드 전체 검토 및 내부 리팩토링 | `develop`에서 `refactor/frontend-clean-code` 브랜치를 생성하고 Flutter `lib/` 전체를 검토한다. 메서드의 단일 책임, 중복 코드, 명명과 가독성, 중첩 깊이 3 이하를 기준으로 리팩토링하되 기존 Riverpod·Repository 구조, 사용자 동작, API·DB 계약, 프로덕션 의존성은 변경하지 않는다. 기존 테스트를 보존·보강하고 포맷·정적 분석·테스트로 검증한다. | 사용자가 새 브랜치 생성과 일반적인 프론트 코드 리팩토링 기준을 명시했다. 동작 및 계약 변경 없이 내부 구조만 개선하는 것이 현재 요청 범위에 부합한다. | Flutter 프론트엔드 소스·테스트·Git 브랜치 | 기존 앱 프론트엔드 요구사항 전체, 협업 규칙 | 사용자 소유 `backend/.vscode/launch.json`과 루트 `README.md`는 수정·커밋하지 않는다. 명세/API/DB 변경이 없으므로 `08-spec-changelog.md`는 변경하지 않는다. |

실행 결과 (2026-09-08): 업로드 화면의 비동기 상태 분기·갤러리 선택·카메라 미리보기·장소 검색 UI를 책임별 메서드로 분리하고, 업로드 컨트롤러의 장소 매칭·검증·요청 초안 생성과 기기 저장소의 미디어 발급·전송·게시 요청·응답 변환을 분리했다. 사진·자유 태그 한도를 단일 상수로 통합했으며 앱 셸 내비게이션과 팔로우 요청의 중첩 콜백을 추출했다. 제한값 회귀 테스트를 추가했다. `dart format .`은 98개 파일 기준 통과, `flutter analyze --no-pub`은 문제 없음, 구분되지 않은 단위·위젯 통합 `flutter test --no-pub`은 74개 전부 통과했다. 신규 의존성·플랫폼 설정·API·DB 계약 변경은 없다.

| DEC-20260908-002 | 2026-09-08 | 사용자 | 확정·실행 요청 | 자동화 검증으로 종료 / 연결된 Android 실기기에서 리팩토링 회귀 검증 | 연결된 Android 실기기에서 리팩토링 영향 동선인 하단 내비게이션, 비회원 로그인 보호, 업로드 갤러리·사진 확인·작성·장소 검색·뒤로가기를 직접 검증하고 런타임 오류 로그를 확인한다. 앱 데이터 초기화, 계정 삭제, 실제 게시물 등록은 수행하지 않는다. 발견된 문제는 기존 구조와 API 계약 안의 회귀일 때만 수정한다. | 사용자가 실기기를 연결하고 리팩토링 결과에 문제가 없는지 직접 테스트하도록 요청했다. 실제 게시 등 외부 상태 변경 없이 리팩토링된 UI와 상태 전이를 검증할 수 있다. | Android 실기기 앱 설치·화면 동선·Flutter 런타임 로그 | DEC-20260908-001, DEC-20260907-015~018 | 신규 의존성·권한·API·DB 계약 변경은 별도 확인 없이 수행하지 않는다. |

실행 결과 (2026-09-08): `SM S938N`(Android 16, API 36)에서 온보딩·로그인·비회원 진입, 하단 내비게이션, 비회원 업로드 보호 모달, 업로드 갤러리·사진 확인·작성 폼·장소 검색·뒤로가기·취소 복귀·카메라 미리보기 동선을 확인했다. 사진 4장 선택 상태와 5번째 선택 제한 안내가 정상 동작했고, 화면 상태도 왕복 이동 후 유지됐다. 실행 중 `FATAL EXCEPTION`, `E/flutter`, 미처리 예외, 레이아웃 오버플로, dispose 이후 setState 오류는 없었으며 프로세스가 유지됐다. 로그인 직후 재시작처럼 보인 현상은 테스트용 APK의 `adb install -r` 수행 시 Android가 기존 프로세스를 종료한 기록과 일치하며 앱 크래시는 아니었다. 로컬 백엔드가 응답하지 않아 성공 API 연동은 테스트 대역으로 UI 회귀만 검증했고 실제 게시물은 등록하지 않았다. 검증 후 dart-define이 없는 일반 디버그 APK로 복원했다. `dart format lib test`, `flutter analyze --no-pub`, `flutter test --no-pub`은 각각 통과했고 테스트 74개가 모두 성공했다. `dart format .` 재실행은 Android 빌드가 만든 플러그인 캐시 경로 열거 오류로 완료하지 못했으나 소스 대상 포맷은 변경 없이 통과했다.

| DEC-20260908-003 | 2026-09-08 | 사용자 | 확정·실행 요청 | 로컬 변경만 유지 / Git 전략에 따라 커밋·원격 푸시·PR 생성 | 프론트엔드 클린 코드 리팩토링과 회귀 테스트, 관련 결정 로그를 기능 단위 커밋으로 생성하고 원격에 푸시한 뒤 `develop` 대상 PR을 템플릿에 맞춰 생성한다. 브랜치 전략의 허용 type에 맞추기 위해 아직 원격에 게시되지 않은 `refactor/frontend-clean-code`를 `chore/frontend-clean-code`로 변경한다. 사용자 소유 `backend/.vscode/launch.json`과 루트 `README.md`는 커밋에서 제외한다. PR merge는 수행하지 않는다. | 사용자가 `docs/09-git-strategy.md`에 따른 커밋·푸시·PR 생성을 명시적으로 요청했다. 현재 브랜치의 `refactor` type은 문서에 정의된 브랜치 type 목록에 없고, 코드 구조 개선에는 `chore` 작업 브랜치와 `refactor(frontend)` 커밋·PR 제목 조합이 가장 가깝다. | Git 브랜치·커밋·GitHub PR·결정 로그 | DEC-20260908-001~002, `docs/09-git-strategy.md` | PR의 대상은 `develop`이며 리뷰·merge는 별도 승인 후 수행한다. |

실행 결과 (2026-09-08): 작업 브랜치를 `chore/frontend-clean-code`로 변경하고 `refactor(frontend): 업로드 흐름 책임 분리`(`409aceb`), `test(frontend): 업로드 선택 한도 회귀 검증`(`15c69cc`), `docs: 프론트엔드 리팩토링 결정 기록`(`f61380e`) 커밋을 생성해 `origin/chore/frontend-clean-code`에 푸시했다. 저장소 PR 템플릿의 작업 영역·작업 내용·테스트 방법·체크리스트·참고 사항을 채워 `develop` 대상 GitHub PR #52(`[refactor](frontend): 프론트엔드 클린 코드 개선`)를 생성했다. 사용자 소유 `backend/.vscode/launch.json`과 루트 `README.md`는 커밋에서 제외했으며 PR은 merge하지 않았다.

## Android 릴리즈 서명 키

| 결정 ID | 날짜 | 주체 | 상태 | 선택지·배경 | 결정 내용 | 근거 | 영향 범위 | 관련 요구사항 | 대체/비고 |
|---|---|---|---|---|---|---|---|---|---|
| DEC-20260909-004 | 2026-09-09 | 사용자 | 확정·실행 요청 | release 빌드의 debug 서명 폴백 유지 / 실제 release 서명 키 생성·연결 | Android release 서명용 PKCS12 keystore를 RSA 4096비트·SHA-256·유효기간 10,000일로 생성하고 alias는 `upload`을 사용한다. 난수 비밀번호와 keystore는 Git 제외 파일인 `app/android/key.properties`와 `app/android/upload-keystore.jks`에만 저장한다. | 사용자가 실제 릴리즈 키 생성을 명시적으로 요청했다. 현재 Gradle 설정은 `key.properties`가 있으면 release signing config를 자동 적용한다. | Android release APK·AAB 서명, Google OAuth release SHA 등록, Play 배포 키 보관 | AUTH-001, Android 배포 | 키와 비밀번호는 로그·문서·커밋에 포함하지 않는다. 키 분실 시 기존 앱 업데이트가 불가능할 수 있으므로 별도 안전한 백업 필요. |

실행 결과 (2026-09-09): release keystore와 난수 비밀번호 설정을 생성했으며, Gradle `signingReport`에서 `Variant: release`, `Config: release`, alias `upload` 및 debug 인증서와 다른 release 인증서가 적용되는 것을 확인했다. 두 비밀 파일은 저장소의 기존 ignore 규칙으로 Git 추적에서 제외된다.

## 마이페이지 로그아웃

| 결정 ID | 날짜 | 주체 | 상태 | 선택지·배경 | 결정 내용 | 근거 | 영향 범위 | 관련 요구사항 | 대체/비고 |
|---|---|---|---|---|---|---|---|---|---|
| DEC-20260911-001 | 2026-09-11 | 사용자 | 확정·실행 요청 | 설정 화면에서만 로그아웃 제공 / 마이페이지에도 로그아웃 버튼 제공 | `develop`에서 `feature/frontend-profile-logout` 브랜치를 생성하고, 본인의 마이페이지에서 기존 `AuthController.signOut()` 흐름을 실행하는 로그아웃 버튼을 제공한다. 다른 사용자의 공개 프로필에는 버튼을 노출하지 않는다. | 사용자가 마이페이지의 로그아웃 버튼 부재를 제보하고 새 브랜치에서 구현하도록 요청했다. 기존 인증·라우팅 계약을 재사용하면 백엔드나 API 변경 없이 일관된 로그아웃이 가능하다. | Flutter 마이페이지 UI·인증 상태·위젯 테스트 | AUTH-005, USER-001 | 백엔드·API·DB 계약과 설정 화면의 기존 로그아웃 기능은 변경하지 않는다. |
| DEC-20260911-002 | 2026-09-11 | 사용자 | 확정·실행 요청 | 중복된 문자열·정수 ID 변환 함수 유지 / 단일 정수 변환 함수로 통합 | 업로드 저장소에 중복 정의된 `_numericId`를 정수 반환 함수 하나로 통합한다. JSON 요청 본문에는 정수를 사용하고, `ApiClient`가 문자열을 요구하는 URL 쿼리에서는 변환 결과를 문자열로 직렬화한다. | 사용자가 전체 분석과 테스트를 막는 중복 정의·타입 오류 수정을 요청했다. 백엔드 DTO는 숫자 ID를 받고 HTTP 쿼리는 문자열로 전달되므로 전송 위치별 타입을 보존해야 한다. | Flutter 업로드 태그 추천·등급 미리보기·게시글 생성 요청 변환 | API-CMU-011, API-PST-002~003 | API·DB 계약은 변경하지 않으며 `docs/08-spec-changelog.md` 적용 대상이 아니다. |

## 운영 서버 Base URL 연동

| 결정 ID | 날짜 | 주체 | 상태 | 선택지·배경 | 결정 내용 | 근거 | 영향 범위 | 관련 요구사항 | 대체/비고 |
|---|---|---|---|---|---|---|---|---|---|
| DEC-20260912-001 | 2026-09-12 | 사용자 | 확정·실행 요청 | 로컬 서버를 기본값으로 유지 / 인프라 제공 고정 IP를 기본 API 서버로 사용 | Flutter의 기본 `API_BASE_URL`을 `http://3.37.39.98`로 변경하고 연결된 Android 실기기에 설치해 실제 API 연동을 검증한다. 서버가 현재 HTTP만 제공하므로 평문 HTTP 허용은 debug 빌드에만 적용하고 release 보안 정책은 유지한다. | 사용자가 인프라 담당자가 제공한 고정 IP로 Base URL을 변경하고 실기기 테스트하도록 요청했다. 사전 점검에서 `GET /actuator/health`와 `GET /api/v1/places`가 80번 포트에서 200으로 응답했으며 443·8080번 포트는 외부에서 응답하지 않았다. | Flutter 기본 API endpoint, Android debug 네트워크 보안 설정, 실기기 검증 | SYS-002, 현재 Flutter API 연동 전체 | API·DB 계약 변경은 없어 `docs/08-spec-changelog.md` 적용 대상이 아니다. release 적용 전 HTTPS endpoint 확정이 필요하다. |
| DEC-20260912-002 | 2026-09-12 | 사용자 | 확정·실행 요청 | 현재 작업 트리에만 유지 / Git 전략에 따라 분리 커밋·원격 푸시·PR 생성 | 운영 서버 Base URL 변경, debug 실기기 연동 설정, 회귀 테스트와 결정 기록만 `chore/frontend-api-base-url` 브랜치에 선택적으로 커밋하고 원격에 푸시한 뒤 `develop` 대상 PR을 저장소 템플릿에 맞춰 생성한다. 현재 작업 트리의 프로필·업로드·Gradle·IDE·README 관련 기존 변경은 커밋에서 제외하고 PR은 merge하지 않는다. | 사용자가 Git 전략에 따른 커밋·푸시·PR 생성을 명시적으로 요청했다. 현재 브랜치와 작업 트리에 다른 작업이 섞여 있어 이번 서버 연동 변경만 선택적으로 분리해야 한다. | Git 브랜치·커밋·GitHub PR·결정 로그 | DEC-20260912-001, `docs/09-git-strategy.md`, `docs/commit-convention.md` | PR 대상은 `develop`이며 리뷰·merge는 별도 승인 후 수행한다. |
| DEC-20260912-003 | 2026-09-12 | 사용자 | 확정·실행 요청 | 서버 연동 변경만 선택 커밋 / 현재 브랜치의 모든 미커밋 사항을 기능별로 전부 커밋 | `chore/frontend-api-base-url`에 남아 있는 프로필 로그아웃, 업로드 ID 직렬화, Android 빌드 도구 설정, 백엔드 VS Code 실행 설정, 루트 README, 운영 서버 연동 및 전체 결정 로그를 Git 전략에 따라 기능별 커밋으로 모두 생성해 푸시하고 하나의 `develop` 대상 PR에 포함한다. 비밀값과 Git 제외 빌드 산출물은 포함하지 않으며 PR은 merge하지 않는다. | 사용자가 현재 브랜치의 미커밋 사항도 전부 커밋·푸시하도록 추가로 명시했다. 사전 검사에서 대상 파일에 API 키·비밀번호·개인 키가 없고 APK 등 빌드 산출물은 ignore 상태임을 확인했다. | Flutter 기능·테스트, Android 빌드 설정, 백엔드 IDE 설정, 루트 문서, Git 브랜치·커밋·GitHub PR | DEC-20260911-001~002, DEC-20260912-001~002, `docs/09-git-strategy.md`, `docs/commit-convention.md` | DEC-20260912-002의 선택 커밋 범위를 대체한다. 현재 브랜치 이름은 최초 서버 연동 작업 기준으로 유지한다. |

실행 결과 (2026-09-12): `http://3.37.39.98`의 Actuator와 공개 장소 API가 200으로 응답하는 것을 확인했다. 변경된 기본값으로 debug APK를 `SM S938N`(Android 16, API 36)에 설치해 앱 실행과 서버 기반 지역 목록 표시를 검증했으며 cleartext·소켓·Flutter 예외는 발생하지 않았다. `dart format lib test`, `flutter analyze`, 구분되지 않은 단위·위젯 통합 `flutter test`를 통과했고 테스트 134개가 모두 성공했다. 전체 `dart format .`은 로컬 SDK 환경에서 출력 없이 정지해 중단하고 소스 디렉터리 대상으로 재실행했다.
