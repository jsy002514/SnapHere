# Android 실기기 검증 — 2026-09-20

- 기준: develop / 683f614, debug 0.1.0+1, android-arm64
- 기기: Samsung Galaxy Note10+ 5G (SM-N976N), Android 12 / API 31, 1440×3040
- 적용 결정: DEC-20260920-001
- 기기 변경: SnapHere 신규 설치 및 앱 내 게스트 탐색. 다른 앱·개인 파일 조작 없음. 게시물·댓글 생성, 좋아요, 팔로우, 계정 변경 없음.
- 빌드 복사본: C:/src/snaphere-device-20260920
- APK: C:/src/snaphere-device-20260920/build/app/outputs/flutter-apk/app-debug.apk
- 로컬 app/.env: Google Maps/OAuth 값 비어 있음. 실제 인증정보를 포함하지 않음.

## 검증 결과

| 항목 | 결과 |
|---|---|
| Android debug APK 빌드·설치·실행 | 통과 |
| 온보딩 → 로그인 화면 → 게스트 진입 | 통과 |
| 커뮤니티 목록·이미지 조회 | 통과 |
| 게시글 상세 | 조회 성공, 장소 행 레이아웃 오류 재현 |
| 댓글 화면 및 댓글 없음 상태 | 표시 확인 (댓글 작성 미실행) |
| 이벤트 목록·이미지·상세 | 조회 확인 |
| 마이·업로드의 게스트 접근 | 로그인 필요 안내 표시 확인 |
| 홈 지역 선택 → 서울 게시글 목록 | 조회 확인 |
| Google 지도 | 키 미설정 안내 확인. 실제 지도·마커 검증 보류 |
| Google 로그인·로그인 이후 기능 | OAuth 설정 부재로 미검증 |
| flutter analyze --no-pub | No issues found |
| flutter test --no-pub | 231개 통과 |
| 운영 서버 /actuator/health | HTTP 200, UP |

## 재현 오류: 게시글 장소명·주소 가로 넘침

- 순서: 로그인 없이 둘러보기 → 커뮤니티 → 장소가 '전북특별자치도 × 전주시청년축제'인 '테스트' 게시글 열기.
- 실제: 장소 행이 오른쪽 화면 경계를 넘으며 debug 경고 `RIGHT OVERFLOWED BY 70 PIXELS` 표시.
- 기대: 긴 장소명·주소가 화면 안에서 줄바꿈 또는 말줄임 처리되어야 함.
- 소스 확인: app/lib/src/features/post/presentation/post_detail_screen.dart의 _PlaceRow (약 167행)에서 Row 안의 장소명·주소 Text에 폭 제약이 없음.
- 증거: C:/src/snaphere-device-20260920-post.png
- 상태: 재현 및 원인 코드 확인, 이번 검증에서 앱 소스 수정은 하지 않음.

## 로컬 실행 환경 문제와 해결

- 원래 프로젝트 경로에서 Gradle 배치 실행 실패: 영문 경로에 앱 복사본을 만들어 빌드 성공.
- Windows flutter_tester가 테스트 로딩 전에 종료(-1073740791): TEMP/TMP를 영문 경로로 지정한 후 전체 테스트 통과.
- 정적 분석도 영문 복사본에서 통과.
- 최초 빌드에서 필요한 NDK 28.2.13676358, Android SDK Platform 35/36, CMake 3.22.1을 기존 승인된 SDK 라이선스로 설치.

## 남은 검증

실제 Google Maps 키 및 서버와 일치하는 OAuth 클라이언트 ID를 로컬 app/.env에 설정하고 재빌드해야 지도와 Google 로그인을 검증할 수 있다. 로그인 후 프로필·업로드·뱃지 등은 아직 검증하지 않았다. 기기 개인 사진을 열거나 변경하지 않는 범위의 별도 테스트 데이터가 필요하다.

## 후속 수정·재검증 — DEC-20260920-002

- 위 게시글 장소 행 오류를 수정했다. 장소명·주소를 가로 폭이 제한된 세로 영역에 배치하여 가용 폭과 시스템 글자 배율에 맞게 줄바꿈한다. 빈 주소는 표시하지 않는다. 장소 상세 이동과 신뢰 배지는 유지한다.
- 화면 크기 320×640, 360×800, 412×893, 800×360, 1024×768 각각에서 글자 배율 1.0, 1.5, 2.0의 15개 조합을 검증했다. 긴 장소명·주소의 가로 경계, 예외 없음, 스크롤로 내용 접근, 장소 상세 이동을 확인했다.
- 전체 Flutter 테스트 252개 통과, flutter analyze 통과, Android debug APK 빌드 성공.
- 연결된 SM-N976N에 앱을 업데이트 설치하고 동일 게시글에서 장소명·주소 정상 표시 및 기존 70픽셀 넘침 경고 제거를 직접 확인했다.
- 수정 후 화면: C:/src/snaphere-device-20260920-responsive-post.png
- 재빌드의 JNI 증분 컴파일에서 한글 Pub 캐시 경로 오류가 발생해 해당 빌드 프로세스의 PUB_CACHE만 C:/src/snaphere-pub-cache로 지정했다. 앱 의존성 버전 변경 없음.
- 기기 시스템 화면 크기·글자 설정과 다른 앱·개인 파일은 변경하지 않았다. 모든 실제 기종에서 검증한 것은 아니며 이번 수정 범위는 게시글 장소 영역이다.
