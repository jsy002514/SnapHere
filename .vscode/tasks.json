{
  "version": "2.0.0",
  "tasks": [
    { "label": "빌드", "type": "shell", "command": "./gradlew build -x test",
      "options": { "cwd": "${workspaceFolder}/backend" }, "problemMatcher": [] },
    { "label": "서버 실행", "type": "shell", "command": "./gradlew bootRun",
      "options": { "cwd": "${workspaceFolder}/backend" }, "isBackground": true, "problemMatcher": [] },
    { "label": "테스트", "type": "shell", "command": "./gradlew test",
      "options": { "cwd": "${workspaceFolder}/backend" }, "problemMatcher": [] },
    { "label": "MySQL 시작 (Docker)", "type": "shell", "command": "docker compose up -d",
      "options": { "cwd": "${workspaceFolder}" }, "problemMatcher": [] }
  ]
}
